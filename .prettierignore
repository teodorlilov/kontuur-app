.next
node_modules
public

# Vendored third-party skills (obra/superpowers, impeccable). Not our code — formatting
# it would rewrite ~170 files we do not maintain and make every upstream re-vendor a
# conflict. This is 38% of what `format:check` used to fail on.
.claude
.impeccable

# Prose. Prettier reflows markdown paragraphs, which turns a one-line edit to any of
# these documents into a whole-file diff and buries what actually changed.
*.md

# Generated by `supabase gen types` — reformatting it means every regeneration
# re-breaks the format gate.
src/types/database.ts
