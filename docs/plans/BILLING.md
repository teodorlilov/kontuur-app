# Billing — implementation plan (Phase 1: enforcement, Phase 2: Stripe)

## Context

Kontuur cannot go live without a way to charge and — more urgently — without a way to *stop* spending
for workspaces that never pay. Today nothing enforces anything: every agency carries `plan='free'`,
`subscription_status='trialing'` and a past `trial_ends_at`; no code path reads those columns for a
decision; all six crons select every agency; the only metered counter (`image_generation_usage`) is
called with a 2-billion ceiling. Four of those gaps are live security holes (tenant-writable plan
columns, self-promotable role, anon-callable usage RPCs, ungated crons).

The pricing model was designed on 2026-09-11 (per-brand plans, pooled allowances that pause rather
than bill overage, app-owned card-free trial) and the founder has said: build with the proposed
numbers as placeholders, change later. The legal research on 2026-09-12 found that direct card
acceptance in Bulgaria triggers Наредба Н-18 (fiscal sale document + monthly audit file) while a
merchant of record does not. So this plan built everything that did not depend on that answer
(Phase 1, shipped), and now — the founder having decided on 2026-09-16, with the accountant's notes
in hand — Stripe **direct** (not a merchant of record) with the Наредба Н-18 *alternative regime*
built in-house: every paid Stripe invoice becomes one Bulgarian-numbered invoice that is also the
Н-18 sale document (чл. 52о ал. 3), delivered at payment time, plus the monthly XML audit file the
founder uploads to the NRA. Both businesses and consumers are customers, so Stripe Tax does the VAT
(OSS once registered) and the checkout carries the consumer-law consent. Everything is in euro. What
still waits (Phase 3) is copy and configuration, not code: the public pricing page and the Terms
rewrite (mock first), the NRA registration and the Stripe Dashboard settings the founder does by
hand, and the switch to live keys.

Written against files opened in this session; see the verified table at the end.
`npm run plan:check` is run against this file before it is presented.

## Progress (2026-09-13)

- **Step 1 applied to production** (`supabase/migrations/20260852_billing_foundation.sql`) and types
  regenerated. Lesson recorded by the review that ran on it: the migration dropped `plan_client_limit`
  and nulled `subscription_status` in the same batch as the additive changes, so the deployed code
  selected a dropped column until the code below shipped — a purely additive first migration, with
  the drops in a later one, would have had no window. The review's other substantive findings are in
  `supabase/migrations/20260853_billing_followups.sql` (admin-only reads of the two new tables, the
  cost column withheld from the tenant role, two integrity checks) — **to apply next**.
- **Steps 2 and 3 built, plus the PlanSection half of step 7** — uncommitted, every gate green
  (typecheck, lint, format, knip, arch, writers, comments, build, 2,084 tests). Two shape changes
  from the plan as written: `generatePostVisual` lets `AllowanceError` propagate instead of adding a
  typed `images_allowance` reason (one refusal type everywhere), and `subscribeFal`'s exported
  wrappers keep their signatures because identity comes from the spender context, not a parameter.
- **Steps 4, 5 and 6 complete** (second batch, uncommitted): the wire caps (`MAX_POSTS_PER_RUN`
  holds `targetPostCount`, `priorityPosts`, `frequency_value`, `posts_per_week`; the cron clamps
  slides); the publish scheduler filters both its queries by the entitled clients ahead of its
  LIMIT, the metrics and comments rosters take one entitled set per tick from their cron route;
  `cron-invariants` proves every cron except `refresh-tokens` (and, since step 8, `billing`) reaches the gate; the brand cap and
  `create` gate in `createClient` (provisioning now through the admin client — apply
  `supabase/migrations/20260854_clients_insert_admin_only.sql` with this deploy), `publish` gates
  on publish-now and scheduling (unscheduling stays open); `gate-coverage` pins the allowlist and
  requires any file calling the AI/visuals limiter to be in it.
- **Step 7 complete** (third batch, uncommitted): `BillingBanner` above every dashboard page for
  a trial in its last three days (Amber), the grace (Clay, with the day publishing stops) and a
  failed renewal (Amber) — derived from the agency row the layout already holds, no usage read
  (the 80 % warning is the bell's job via `consumeUsage`); `BillingWall` replaces every page but
  Settings when locked; the (generate) and (onboarding) layouts redirect a workspace that may not
  spend or create to Plan & billing; the wizard's visuals hook treats a 402 as an allowance
  refusal with the server's sentence instead of the retry toast (the editor and the wizard's
  stream already surface the 402 body's `error`). The calendar's "paused" pill was dropped: the
  wall already says it on that page.
- **Fourth batch, from the founder's first test pass** (uncommitted): a refusal is honest about a
  partial allowance — `AllowanceError` carries `needed`, so "2 left, this needs 3" replaces "all
  60 used"; the wizard receives `draftsLeft` from the server render, caps its stepper at it and
  disables Generate with the sentence and the Plan & billing link, the cron writes a partial batch
  (`draftBudgets`, one `readUsage` per entitled agency per tick) instead of skipping the client.
  Only what exists is charged: the rewrite route refunds when the model call throws, and every
  image boundary (the three visual routes, inpaint, SVG, the visuals cron) counts the images fal
  actually returned on `spender.charged` and releases them with `releaseCharged` when the request
  fails after the reservation. "Add client" is disabled where it stands with the reason beside it
  (`AddClientAction`, over the same `addBrandRefusal` rule `createClient` applies) rather than
  redirecting a member through the form. Analytics for a workspace that cannot spend shows the
  non-AI sentence both narratives already fall back to (`buildFallbackNarrative`,
  `buildFacebookFallbackNarrative`); the wall covers the locked state.
- **Review batch** (2026-09-14, uncommitted; the by-hand review's 24 findings): the solo
  redirect loop closed — `requireBusinessSetup` takes `canCreate` and leaves a lapsed workspace
  on the wall instead of bouncing it to onboarding and back; a refused rewrite now reaches the
  person in the route's own words (`rewriteDraft` returns the 402 body's sentence); the 80 % bell
  can no longer undo a reservation (`consumeUsage` catches its own notify); the cron's exhausted
  bell reads the budget it draws down (`DraftBudget {used, quota}`, one `notifyDraftsExhausted`);
  the trial no longer promises a reset (`resetsOn` is null on the trial and the sentence ends
  "Choose a plan to keep generating."); the entitlement carries `timezone` and `graceEndsAt`, so
  every date it says — the banner, Settings, a 402, a bell — is the customer's day and past_due
  says by when the card is due; `allows()` and the entitlement derivation each exist once
  (`getCachedEntitlement` in the layout and the roster); every wizard sentence comes from copy.ts;
  `ALLOWANCE_WARN_SHARE`, `meteredLimit` and `PLAN_AND_BILLING_PATH` are defined once; the
  visuals cron budgets the slots still missing and reads usage only for agencies with posts
  loaded; `paste-from-url` is no longer gated (it spends nothing) and gate-coverage now keys on
  `runAsSpender` with an exempt list; a paid row with no period start is locked rather than let
  into the trial bucket; the thing a plan counts is called what the app calls it — "client" (or
  "business" in solo) on every surface, "brand" only here and in the price table. Cut from step 7,
  recorded: the "Ends on" label and the cancel banner land with the webhook that sets
  `cancel_at_period_end` (step 9); the `busy-hint` sentence is not needed — the editor shows the
  402 body's own sentence through `parseAssetResponse`; the `ShellProvider` hand-off was never
  needed, the banner and the wall derive from the layout's read. New tests: fal metering,
  `callAnthropic` fail-closed, `fetchEntitledClients`, the gate pair, the wire caps, the publish
  scheduler's entitled filter, `PlanSection` per state, `rewriteDraft`, and the loop rule.
- **Step 8 complete** (uncommitted): `src/app/api/cron/billing/route.ts` runs daily at 08:00 UTC
  (`vercel.json`) and calls `remindTrialWorkspaces` (`src/lib/billing/reminders.ts`): one read of
  the agencies with no subscription, `pickReminder` (pure) picks the moment — the trial's last three
  days, its grace, or the seven days after the grace ran out — and the sentence is the shell's own
  (`shellNotice`, `workspacePaused`), dated, so `notify` dedups it; the email goes only behind a
  bell row that was actually written (`notify` now says whether it wrote, held back or failed),
  to the workspace's admins — the admins are read before anything is written, so a failed read
  leaves no row behind; one users read per tick. `sendEmail` is the one Resend path —
  `sendApprovalEmail` is gone and the approval sender composes `approvalEmail` itself;
  `resolveAppUrl()` in `src/utils/url.ts` replaces
  the three copies of the origin fallback. The bell titles billing rows as the workspace's own and
  sends them to Plan & billing. `cron-invariants` lists `billing` as the second exemption;
  `unauthorizedCron` (`src/lib/cron/authorize-cron.ts`) is the bearer check all seven cron routes
  now share, with its own test, as `notify`'s three answers now have theirs. Tests:
  `reminders.test.ts` (selector and runner), the three reminder snapshots, the bell row.
- **Phase 2 decided** (2026-09-16, nothing built yet): Stripe direct, both customer types, the Н-18
  documents and audit file in-house, euro only. Steps 9–12 below were written from the code as it
  stands after step 8, from the regulation text (ДВ 8/2020 as quoted by news.inbalance.bg) and from
  the NRA's own `dec_audit.xsd` (06.06.2022, downloaded and read). Two accountant answers are folded
  in as assumptions and flagged where they matter: the invoice may serve as the sale document when
  it carries чл. 52о ал. 1 т. 3–6 (the accountant's note said "issue both"; the regulation says one
  is enough), and the audit file's payment code for Stripe is 2 (virtual POS).
- **Pricing simplified** (2026-09-17, from the founder's review of this plan): one paid plan at
  €19 per brand with no minimum replaces Starter and Agency, whose only difference was the brand
  count. Step 9 lost the plan-change flow and the two brand rules with it; step 10's migration
  narrows the `plan` CHECK; the Phase 1 code that knew two paid plans is migrated in step 9, caller
  by caller (listed there).
- **Second review** (2026-09-17, two independent passes — simplicity and correctness — every
  finding verified in the files before it was folded in): cut the catalogue script, the documents
  download route, the daily quantity reconcile, the admin-guard wrapper, the customer-to-agency
  read and two webhook events; the event dedup now lets a failed event run again on Stripe's
  retry; no ordering by `event.created` (Stripe says not to); the period is filled on the first
  snapshot and advanced only on `invoice.paid`; `error_if_incomplete` on a quantity increase;
  no deferred trial end at checkout; credit notes only for post-payment refunds; the counter
  table on `POLICYLESS`; an idempotency key on the document email; the SDK's pinned API
  version named rather than assumed.

- **Steps 9–12 built** (2026-09-17; uncommitted by the founder's instruction). Stripe:
  `src/lib/billing/stripe.ts`, `subscription-store.ts` (the one writer of the billing columns —
  first-fill and paid-only period rule, ownership by subscription id), `checkout.ts`,
  `quantity-sync.ts` (absolute count, `error_if_incomplete`, credit on a failed provision), the
  two settings actions on the cached role `resolveActionAuth` now returns, `PlanActions` with the
  captured return flag and the bounded poll, the webhook with its re-run-on-retry dedup and the
  six events, the sync in `createClient` / `deleteClient` and the cost line on Add client.
  Documents: migration `20260855_billing_documents.sql` (plan CHECK → `pro`, `document_counters`
  seeded from the accountant's range, `sale_documents`, `issue_sale_document`, the private
  bucket — the two tables and the RPC added to `src/types/database.ts` by hand in the
  generator's layout, which `npm run db:types` replaces identically once applied),
  `document-schemas.ts`, `documents.ts` (VAT basis from the rate's country, credit notes for
  post-payment refunds only, delivery once with the Resend idempotency key, the daily retry, the
  signed download links), `document-render.ts` with the Приложение 18а QR, `render/pdf.ts`,
  `sendEmail` attachments, `documentEmail`, `BillingDocuments`. The audit file: `audit-file.ts`
  held to ASCII, `GET /api/billing/audit-file?month=` behind the cron bearer, `docs/n18/` with
  the NRA's XSD, its sample and the runbook. Step 12: `endsOn` through the entitlement, the shell
  and Settings; `remindWorkspace` shared by the cron and the webhook's `remindPaymentFailed`;
  `BillingReminderType` widened; `CHECKOUT_CONSENT`; `COMPANY` and `BILLING_DOCUMENTS_BUCKET` in
  constants; `formatMoney` and `formatDocumentNumber`; `getZonedParts.second`; the Chromium
  tracing for the two new routes; `.env.example`. Tests: subscription store, quantity sync,
  billing actions, webhook, plan actions, documents, renderer (QR + two HTML snapshots),
  document and payment-failed email snapshots, the documents list, audit file (+ XML snapshot),
  its route, `remindWorkspace` / `remindPaymentFailed`, the client-actions sync, `endsOn`.
  **Still by hand:** apply `20260854` (after the deploy) and `20260855` (with the accountant's
  number range, before the code that writes `pro`), the Dashboard and NRA work of step 12, the
  four accountant confirmations, then the test-mode end-to-end run of step 9.

## Placeholders (one constants file, `src/lib/billing/plans.ts` (new))

| Plan | Price (net) | Brands | Per brand per period, pooled |
|---|---|---|---|
| trial (14 days, no card) | — | 3 in agency mode, 1 in solo | 20 AI drafts · 50 AI images · 15 rewrites, for the whole trial |
| house (the company's own and partner workspaces; set by hand, never from the app) | — | no cap | unmetered — counted, never refused; never locks |
| pro (the one paid plan, decided 2026-09-17; label "Pro" is a placeholder) | €19 per brand per month, no minimum — a solo pays for 1, an agency for each brand it manages | any | 40 · 120 · 30 per paid brand |

The Starter / Agency split this table used to carry is gone: the tiers differed only by the brand
count, so one plan billed per brand says the same thing in one number. The `agencies.plan` CHECK
becomes `('trial', 'pro', 'house')` in migration 20260855 (no row carries `starter` or `agency` —
nothing has ever been sold; the migration asserts that before it changes the constraint).

Grace after an expired trial: 7 days (nothing spends, scheduled posts still publish). Grace after a
failed card: 7 days of full access against the current period's remaining allowance. Then read-only.
Existing production workspaces get a fresh 14 days from the day the migration lands.

## What the user sees when this plan is done

- **Signing up — unchanged.** No card. The workspace starts a 14-day trial: up to 3 brands (1 in solo mode) and a trial allowance of 20 AI drafts · 50 AI images · 15 rewrites per brand, pooled.
- **Settings › Plan is real.** Plan and its date ("Trial · ends 27 September" / "Pro · renews on 14 October"), and four meters: brands, AI drafts, AI images, rewrites — used of allowed, resetting on the renewal date. A quiet banner when the trial has ≤ 3 days left or any meter passes 80 %; an email 3 days before the trial ends and one on the day.
- **Running out.** The wizard refuses with the reset date instead of starting; autopilot pauses for the workspace with a bell notification; the editor's AI-image tools say the allowance is used (upload still works). Editing, approving, scheduling, publishing and analytics keep working — an allowance only pauses *generation*, never bills extra.
- **Trial over → 7 days of grace.** "Your trial ended on … — choose a plan to keep generating." Nothing AI runs; posts already scheduled still publish; comments and analytics keep syncing.
- **After grace → paused.** A wall over the dashboard with one button, "Choose a plan"; everything readable, Settings reachable; the generate and new-brand pages redirect to Settings › Plan; scheduled posts wait.
- **Choosing a plan (Phase 2, Stripe test mode).** One plan: €19 per client per month, billed for the clients the workspace has (one for a solo workspace). Stripe's hosted checkout (card, address, VAT ID for businesses, the consent tick) → back in Settings within seconds with the plan active and the meters reset. Adding a client says "Adds €19 a month, charged pro rata today" before it is created; deleting one lowers next month's price, never below one. "Manage billing" opens Stripe's portal (card, address, cancel at period end). A failed renewal: "Payment failed — update your card by …", 7 days of full access, then paused. Cancel: "Your plan ends on …", then paused with data kept. Every payment produces one document by email within a minute: a Bulgarian invoice (ten-digit number, the VAT line with its legal basis, the Stripe transaction reference and the NRA QR code) that is also the Н-18 sale document; Settings › Plan & billing lists them for download. A refund produces a credit note the same way.
- **Existing users on release day** get a fresh 14-day trial from that day; nobody is locked out by the deploy.
- **Not visible yet (Phase 3):** a public pricing page and rewritten Terms/Privacy (mock first), and the first live charge — the NRA e-shop registration, the Stripe Dashboard configuration and the OSS registration are the founder's hand work, listed in step 12. The cost telemetry is for us, not shown to users.

## Not in this plan (deliberately)

- Public pricing page and Terms/Privacy copy — outside the dashboard, so mock → agree → build (Phase 3). The checkout's consent sentence is the one legal text this plan carries, and its final words are the lawyer's.
- A merchant-of-record flip — rejected 2026-09-16 in favour of the alternative regime; the module boundary keeps it possible later.
- Stripe Tax registrations, the Приложение 33 e-shop registration, the OSS registration, the Dashboard's email and portal settings — configuration the founder does by hand; step 12 lists it in order.
- Wizard "paint on keep" (visuals fire for every streamed draft today) — a product change the founder has not confirmed; nothing here depends on it.
- Seat limits, source limits, briefing counters (the weekly brief is one global row since migration `20260851_globalise_intelligence_briefings.sql`, so there is no per-agency briefing cost any more).

## Shape

One derived truth: `entitlementFor(agencyRow, now)` (pure) reads the `agencies` row every dashboard
render already fetches through `getCachedAgency` and returns `{ state, canSpend, canPublish,
canCreate, plan, quantity, limits, periodKey, trialEndsAt }`. Nothing stores the state twice and no
cron flips a status column. Every gate calls that and nothing else:

- humans → one line per cost-bearing route/action (`requireEntitledRoute` / `requireEntitledAction`), guard-tested;
- crons → the roster is filtered to entitled clients **in SQL, before the LIMIT** (visuals, publish) or in memory where there is no LIMIT (metrics, comments); `refresh-tokens` stays ungated on purpose;
- money → an atomic compare-and-set `consume_usage` RPC (the generalisation of today's `consume_image_credits`) at the two chokepoints where spend is born: `startGenerationRun` (drafts) and `subscribeFal` (images). Quota 0 when the workspace cannot spend — so even a roster that slips through spends nothing.

All Stripe calls live under `src/lib/billing/` and `src/app/api/billing/`; the rest of the app never
imports `stripe`. That module boundary — not an interface — is what keeps a later Managed Payments
flag or a Paddle swap contained.

## How usage is tracked — two layers, one identity

**Layer 1 — what the customer is limited on (the allowance).** Three units, counted atomically in
`usage_counters` by the `consume_usage` compare-and-set, always *before* the money is spent:

| Unit | Counted where | What one unit bundles |
|---|---|---|
| AI draft | `startGenerationRun` reserves `targetCount` at run open; `finishGenerationRun` refunds `target − produced` | the whole text pipeline for one post: the research planner's share, the Sonnet writer, the Haiku judge, the proofreader, up to three revision rounds (≈ 5–8 Claude calls, ≈ €0.05–0.08) |
| AI image | `subscribeFal`, one per paid fal call — gpt-image-2 generation (one per carousel slide), inpaint edit, vector; cutouts free | one fal call (≈ €0.05–0.07) |
| Rewrite | the rewrite route, one per call | one Sonnet rewrite plus its re-validation (≈ €0.03–0.05) |

Claude calls are deliberately **not** the customer-facing unit: a buyer cannot predict "5–8 calls per
post", and the market research ranks opaque credit burn as the top complaint. Everything else Claude
does (source suggestions, brand analysis, slop checks, analytics narrative, style memo) is small,
stays rate-limited, and is gated — a locked workspace cannot call it — but not metered.

**Layer 2 — what it actually cost us (telemetry).** Today nothing records Anthropic token usage per
agency, and the fal editor calls record nothing at all — the invoices are the only view, and they
cannot say which customer or which feature burned what. So every provider call adds to one aggregate
table, `ai_usage_daily` (agency · day · provider · model · flow → calls, input/output/cache tokens,
cost), through an add-only upsert RPC. Written from the three wrappers every paid call already goes
through: `callAnthropic` (the final `Message` carries `usage`), `subscribeFal`, `queryTavily`. Cost is
computed from one price constant (`src/lib/billing/ai-prices.ts` (new)) and stored in **euro cents** —
the only currency Kontuur ever stores, shows or charges. The one place a dollar appears is that
constant: Anthropic, fal and Tavily invoice Kontuur in US dollars, so it carries their dollar list
prices and one USD→EUR rate, applied at write time; no dollar amount reaches a row, a screen or a
customer. The rate is re-read against each month's provider invoices — list prices and the rate
both drift, so the value is relative: cost per customer, per feature, against what the plan
assumed (≈ €2.2 typical, ≈ €10 heavy per brand-month). Never a gate, never throws; a failed write
is a warning.

**One identity for both layers.** `callAnthropic` has fourteen callers deep in `src/ai` with no agency
in scope, and threading an id through all of them would be a cross-layer change touching most of the
engine. Instead the *boundary* that already knows who is spending — each gated route, the spending
action, each cron's per-client loop — declares it once: `runAsSpender({ agencyId, clientId, flow },
fn)` over Node's `AsyncLocalStorage`, and the three wrappers read `currentSpender()`. Fail closed: a
fal or Claude call with no spender in scope is refused (the global weekly brief is the one named
exception — it belongs to nobody and is logged with a null agency). This replaces `generateVisual`'s
explicit `spender` parameter, so "who is spending" is established in exactly one way.

Known check: the analytics narrative runs inside `unstable_cache` (`src/features/analytics/lib/instagram/narrative.ts:116`);
whether the async context survives that boundary is verified by an observed run, and if it does not the
narrative's `resolveNarrative` receives the spender explicitly.

## Module map — one place for each thing, nothing twice

Every new module has one responsibility and one home; every gate, read and write below exists once and
is composed, never copied. The existing helpers it builds on are named so nothing is re-implemented.

| Module | Owns — the one thing | Composes | Called from |
|---|---|---|---|
| `src/lib/billing/plans.ts` (new) | the plan table: prices, allowances, caps, minimum quantity | — | entitlement, Settings, checkout, catalogue script |
| `src/lib/billing/entitlement.ts` (new) | `entitlementFor(row, now)` — the ONLY interpretation of the agencies row | `plans.ts` | everything below; nobody else reads `plan`/`trial_ends_at`/`subscription_status` |
| `getCachedEntitlement` in `src/lib/queries/cache.ts` (existing file) | the per-request entitlement, derived from `getCachedAgency` — **no second agencies read** | `getCachedAgency`, `entitlementFor` | routes, actions, layouts, pages |
| `src/lib/billing/entitled-clients.ts` (new) | `fetchEntitledClients(admin, need)` → `Map<clientId, { agencyId, entitlement }>` — one agencies read + one clients read **per cron tick** | `entitlementFor` | visuals, publish, metrics, comments crons (generate uses its widened `fetchScheduleContext` instead — one read there too) |
| `src/lib/billing/spend-context.ts` (new) | `runAsSpender` / `currentSpender` — who is spending, declared once at the boundary | `AsyncLocalStorage` | gated routes, spending action, cron loops |
| `src/lib/billing/usage.ts` (new) | `consumeUsage` / `refundUsage` / `readUsage`, `AllowanceError`, `allowanceResponse` (the one 402 shape), the one 80 % notification | `consume_usage` RPC, `notify` | `startGenerationRun`, `subscribeFal`, rewrite route, Settings, wizard readout |
| `src/lib/billing/telemetry.ts` (new) + `ai-prices.ts` | `recordAiUsage` and the one price/rate constant | `add_ai_usage` RPC, `currentSpender` | `callAnthropic`, `subscribeFal`, `queryTavily` |
| `src/lib/billing/require-entitled.ts` (new) | `requireEntitledRoute` / `requireEntitledAction` — the one gate pair, same shapes as `aiRateLimitResponse` / `ActionResult` | `getCachedEntitlement` | one line per cost-bearing route/action |
| `src/lib/billing/copy.ts` (new) | every user-facing billing sentence | — | Settings, banner, wall, wizard, editor, e-mails |
| `src/lib/billing/stripe.ts` (new) | the one Stripe client, pinned version | — | checkout, portal, webhook, `changePlan`, quantity sync, catalogue script |
| `src/lib/billing/subscription-store.ts` (new) | `ensureStripeCustomer`, `applySubscriptionSnapshot` — the ONE writer of the billing columns | admin client, `revalidateTag` | webhook, checkout |
| `recordBillingEvent` / `finishBillingEvent` in `src/app/api/billing/webhook/route.ts` (new) | idempotency + audit — two five-line functions with one caller, so they live with it | admin client | the webhook |
| `src/lib/billing/quantity-sync.ts` (new) | `syncSubscriptionQuantity` — the one place the brand count reaches Stripe, always as the absolute count | `stripe.ts` | `createClient`, `deleteClient` |
| `sendEmail` in `src/lib/email/resend.ts` (existing file) | the one Resend send path — gains `attachments` for the document PDF | — | approval e-mail, reminders, payment-failed, documents |
| `COMPANY` in `src/utils/constants.ts` (existing file) | the company's identity on every document — legal name, ЕИК, VAT number, address | — | documents, audit file |
| `src/lib/billing/documents.ts` (new) | `issueSaleDocument` / `issueCreditNote` — the ONE writer of `sale_documents`, through the `issue_sale_document` RPC; `deliverSaleDocument` (render → private bucket → email, once); `retryUndeliveredDocuments` | `stripe.ts`, `renderSaleDocumentHtml`, `renderPdf`, `sendEmail` | webhook, the daily retry |
| `src/lib/billing/document-render.ts` (new) | `renderSaleDocumentHtml(row)` and `qrPayload(row)` — the invoice / credit-note markup with the Приложение 18а QR; pure | `qrcode` | `deliverSaleDocument`, snapshot tests |
| `src/lib/render/pdf.ts` (new) | `renderPdf(html)` — HTML to A4 PDF on the warm Chromium | `getBrowser` | `deliverSaleDocument` |
| `src/lib/billing/audit-file.ts` (new) | `buildAuditFile(…)` — the month's XML per `dec_audit.xsd`; pure | `escapeHtml` | the audit-file route, tests |
| `src/app/api/billing/audit-file/route.ts` (new) | the founder's download of a month's file, behind the cron bearer | `unauthorizedCron`, `buildAuditFile` | the monthly runbook |
| `src/lib/billing/checkout.ts` (new) | the two Stripe calls a person can trigger — `createCheckoutSession`, `createPortalSession` | `stripe.ts`, `subscription-store.ts` | the settings actions |
| `src/features/settings/actions/billing-actions.ts` (new) | `startCheckout`, `openBillingPortal` — auth (the cached role `resolveActionAuth` now returns), the one rule, one call each | `resolveActionAuth`, `checkout.ts` | `PlanActions` |
| `remindWorkspace` in `src/lib/billing/reminders.ts` (existing file) | the bell-then-email pair, extracted from `remindTrialWorkspaces`; takes the admin emails | `notify`, `sendEmail`, `reminderEmail` | the billing cron, the webhook's `payment_failed` |
| `BillingDocuments` in `src/features/settings/components/billing-documents.tsx` (new) | the admin's list of documents with a signed URL per row, minted at render | admin storage, `fetchSaleDocumentsByAgency` | the settings page |

Reads and requests that must stay single:
- One entitlement per request: layouts, pages and the settings return page read it through `getCachedEntitlement`; the wizard's "this run uses N of M" and the editor's allowance state arrive as **props from the server render** — no new browser fetch for them.
- The settings page today reads the agency twice (`getCachedAgency` and `fetchAgencyById` at `src/app/(dashboard)/settings/page.tsx:20-22`); step 7 folds that to one read while it is being rewritten.
- One Stripe call per user action: a brand add is one `subscriptions.update`; a plan change is one preview + one update; the webhook re-fetches the subscription once per event and writes through one function.
- One roster read per cron tick, then one filter; never a per-client entitlement query inside a loop.
- No new UI primitive: every surface in step 7 is built from the shared components DESIGN.md lists (`FormSection`, `StatusPill`, `Button`, `Card`, `SectionHeading`, the page header) and the existing tokens.
- Phase 2 adds no second read of anything that is read once today. The settings page keeps its ONE uncached agency read (`fetchAgencyById`) and `PlanActions` and `BillingDocuments` are fed from it plus one new `fetchSaleDocumentsByAgency`; no billing action reads `agencies` — the rules come from `getCachedEntitlement`, and `countClientsByAgency` is the one brand count; the webhook makes one Stripe re-fetch per event and one `agencies` write, and takes the agency from the subscription's metadata — no lookup; a document is one RPC, one render, one upload, one email; the audit file is one `sale_documents` read per month. No new `.from('agencies')`, `.from('clients')` or `.from('users')` appears outside the helpers that already hold them (`db.ts`, `cache.ts`, `reminders.ts`, `entitled-clients.ts`).

## Steps

Each step ends with its check. `npm run check` after every step; it is not the success criterion —
the named test or observed run is.

Each step also starts the same way, and this is not optional (docs/CLAUDE.md §1 and §3): every
file the step touches is read in full before it is changed; every signature that changes has its
callers listed file:line with a per-caller decision, and every caller is migrated in the same
change; before any new helper, query or request, the three-part survey — `ls` the target directory
and read its exports, grep by shape, grep three synonyms — and the existing operation in
`docs/OPERATIONS.md` is called rather than forked. Nothing in the steps below is assumed from a
summary: the verified table at the end was filled from reads, and the review pass on this plan
already replaced four proposed helpers with existing ones (`escapeHtml` for XML, `toDateKey` and
`getZonedParts` for the QR's date and time, `formatLongDate` for the printed date, the admin guard
that `removeTeamMember` carried inline).

### 0. Put the plan in the repo
Copy this file to `docs/plans/BILLING.md` (new). → verify: `npm run plan:check -- docs/plans/BILLING.md` green.

### 1. Migration `supabase/migrations/20260852_billing_foundation.sql` (new) + types + registry — ships first, because every TypeScript gate is bypassable until the database stops trusting the tenant

SQL, in order:
1. `agencies.trial_ends_at` → `timestamptz USING trial_ends_at AT TIME ZONE 'UTC'` (default stays `now() + 14 days`). Same bug class `20260843_scheduled_at_timestamptz.sql` fixed for posts.
2. `UPDATE agencies SET plan='trial' WHERE plan='free' OR plan IS NULL`; `plan` default `'trial'`, `CHECK (plan IN ('trial','starter','agency'))`.
3. `subscription_status`: default `NULL`; existing rows with no `stripe_subscription_id` set to `NULL`; `CHECK (subscription_status IS NULL OR subscription_status IN ('trialing','active','past_due','canceled','unpaid','paused','incomplete','incomplete_expired'))`.
4. Add `subscription_quantity integer`, `current_period_start timestamptz`, `current_period_end timestamptz`, `cancel_at_period_end boolean NOT NULL DEFAULT false`, `past_due_since timestamptz`, `billing_updated_at timestamptz`.
5. `DROP COLUMN plan_client_limit` — limits live in `plans.ts`; its only readers are `PlanSection`, `AgencyInfo` and the two projections, all rewritten below.
6. Grace backfill: `UPDATE agencies SET trial_ends_at = GREATEST(trial_ends_at, now() + interval '14 days') WHERE stripe_subscription_id IS NULL`.
7. Tenant lock: `REVOKE INSERT, UPDATE, DELETE ON public.agencies FROM anon, authenticated; GRANT UPDATE (name, timezone) ON public.agencies TO authenticated;` — the settings `PUT` writes exactly those two columns through the user-scoped client (`src/app/api/settings/account/route.ts:53`). `REVOKE INSERT, UPDATE, DELETE ON public.users FROM anon, authenticated;` — both users writers use the admin client (`createUserRecord`, `removeTeamMember`). Policies unchanged. (The `clients` INSERT revoke belongs to step 6, in its own migration, because `createClient` inserts through the user-scoped client until that step lands — revoking it here would break brand creation between the two deploys.)
8. `CREATE TABLE usage_counters (agency_id uuid NOT NULL REFERENCES agencies ON DELETE CASCADE, period text NOT NULL, kind text NOT NULL CHECK (kind IN ('draft','image','rewrite')), count integer NOT NULL DEFAULT 0, PRIMARY KEY (agency_id, period, kind))`; RLS on; policy `usage_counters_agency_isolation` FOR SELECT (member reads own agency, same predicate shape as `agencies_member_access` in `supabase/migrations/20260818_capture_rls_policy_baseline.sql:31`); no user-scoped writes.
9. RPCs `consume_usage(p_agency_id uuid, p_period text, p_kind text, p_cost int, p_quota int) RETURNS TABLE(allowed boolean, used integer)` and `refund_usage(p_agency_id, p_period, p_kind, p_cost)` — the body of `consume_image_credits` (`supabase/migrations/00000000_baseline.sql:1090-1117`) with the `kind` column. **`REVOKE EXECUTE ON FUNCTION … FROM PUBLIC, anon, authenticated; GRANT EXECUTE … TO service_role`** — today's RPCs are SECURITY DEFINER with the default EXECUTE TO PUBLIC and no migration in the repo has ever revoked anything.
10. `INSERT INTO usage_counters SELECT agency_id, month, 'image', count FROM image_generation_usage`; a `DO` block that RAISEs if the counts differ; `DROP FUNCTION consume_image_credits, refund_image_credits; DROP TABLE image_generation_usage`.
11. `CREATE TABLE ai_usage_daily (agency_id uuid REFERENCES agencies ON DELETE CASCADE, day date NOT NULL, provider text NOT NULL CHECK (provider IN ('anthropic','fal','tavily')), model text NOT NULL, flow text NOT NULL, calls integer NOT NULL DEFAULT 0, input_tokens bigint NOT NULL DEFAULT 0, output_tokens bigint NOT NULL DEFAULT 0, cache_read_tokens bigint NOT NULL DEFAULT 0, cache_creation_tokens bigint NOT NULL DEFAULT 0, cost_eur_cents bigint NOT NULL DEFAULT 0, PRIMARY KEY (agency_id, day, provider, model, flow))` — aggregate, so it never needs a retention sweep (≤ ~15 rows per agency per day); a null-agency row set for the global brief via a separate unique index; RLS on, `ai_usage_daily_agency_isolation` FOR SELECT by agency. RPC `add_ai_usage(…)` = insert-or-add on conflict, SECURITY DEFINER, `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated; GRANT … TO service_role` like the other two.
12. `CREATE TABLE billing_events (id text PRIMARY KEY, type text NOT NULL, created timestamptz NOT NULL, object_id text, agency_id uuid REFERENCES agencies ON DELETE SET NULL, payload jsonb NOT NULL, processed_at timestamptz, error text)`; index `(object_id, created DESC)`; RLS on; policy `billing_events_agency_isolation` FOR SELECT by agency (keeps `POLICYLESS` in `src/app/__tests__/rls-policies.test.ts:55` empty). Written only by the webhook (Phase 2).
13. `notify pgrst, 'reload schema'`.

Code in the same change:
- Apply to prod in the SQL editor after a backup (the CLI does not track this project's history — see the `db:push` note in `docs/CLAUDE.md`), then `npm run db:types` and commit `src/types/database.ts`.
- `AgencyInfo` (`src/types/api.ts:324`) becomes `Pick<AgencyRow, …>` of the regenerated row with `plan`/`subscription_status` narrowed to the CHECK unions; delete `'types/api.ts:AgencyInfo'` from `KNOWN_MIRRORS` (`src/types/__tests__/row-mirrors.test.ts:96-100`, the backlog that may only shrink); drop the cast in `fetchAgencyById` (`src/lib/queries/db.ts:116-128`).
- `AGENCY_COLUMNS` / `AGENCY_SETTINGS_COLUMNS` (`src/lib/queries/select-columns.ts:235-239`) carry the new columns and lose `plan_client_limit`.
- `NotificationType` (`src/types/api.ts:292`) gains `'allowance_warning' | 'allowance_reached' | 'trial_ending' | 'trial_ended' | 'workspace_paused' | 'payment_failed'`.
- `scripts/table-writers.json`: `usage_counters` (hand-listed — written through `.rpc()`, which the registry's docblock says it cannot see) → `src/lib/billing/usage.ts` (new); `ai_usage_daily` (hand-listed, same reason) → `src/lib/billing/telemetry.ts` (new); `billing_events` → `src/app/api/billing/webhook/route.ts` (new, Phase 2 — the one writer); `agencies` += `src/lib/billing/subscription-store.ts` (new, Phase 2). `docs/OPERATIONS.md`: remove the row "Record image spend | `recordImageSpend`"; add "Reserve monthly allowance | `consumeUsage`" and "Refund a reservation that produced nothing | `refundUsage`" under Generation; Phase 2 adds the Account rows.

→ verify: migration applies on a fresh shadow DB and on a prod copy; `SELECT count(*) FROM usage_counters WHERE kind='image'` equals the old table's count; a PostgREST `UPDATE agencies SET plan='agency'` with the anon key returns 42501 while `PUT /api/settings/account` still saves name and timezone (extend the existing RLS guard test with the grant assertions); `npm run typecheck`, `npm run writers -- --check`, row-mirrors and rls-policies tests green; every existing agency reads `plan='trial'` with `trial_ends_at >= now()+14d`.

### 2. Plans, the entitlement helper, the cron roster helper (pure, clock-injected tests)
- `src/lib/billing/plans.ts` (new): `PLANS` — prices, per-brand allowances, brand caps, `agency` minimum quantity 3, trial allowances keyed on `mode`.
- `src/lib/billing/entitlement.ts` (new): `entitlementFor(row, now)` → states `trial | trial_grace | active | past_due | locked`; `active` requires `subscription_status='active'` — a Stripe `'trialing'` subscription (an early converter with a deferred `trial_end`) keeps **trial** limits until the first paid invoice lands; `past_due` grace counts from `past_due_since`, not from `current_period_end` (Stripe advances that on the renewal invoice); `periodKey` = `'trial'` before any subscription, else the ISO date of `current_period_start`; limits = per-brand × `max(subscription_quantity, minimum)` on agency, ×1 on starter, trial constants by mode; all zero when the workspace cannot spend.
- `getCachedEntitlement(agencyId)` beside `getCachedAgency` in `src/lib/queries/cache.ts:41-55` — `entitlementFor(await getCachedAgency(id))`, so it inherits the 60 s `'agencies'` tag; anything that changes the row must `revalidateTag('agencies')` (the settings `PUT` already does at `src/app/api/settings/account/route.ts:59`).
- `src/lib/billing/entitled-clients.ts` (new): `fetchEntitledClients(admin, need)` — one read of every agency's billing columns (the table is tens of rows), `entitlementFor` each, one read of `clients.id, agency_id` for the entitled agencies → `Map<clientId, { agencyId, entitlement }>`, so a cron can spend against the right entitlement without a second read. Note in its doc: an `.in()` filter carries the ids in the URL; past a few hundred clients this becomes a flag column, not a bigger list.

→ verify: `src/lib/billing/__tests__/entitlement.test.ts` (new) covers every state × need, both modes, the Stripe-`trialing` case, past_due inside/after 7 days, cancel_at_period_end; `entitled-clients` test asserts one `.from('agencies')` and one `.from('clients')` call for a fixture across three agencies.

### 3. Spender context, usage ledger, telemetry — metering at the three wrappers
- `src/lib/billing/spend-context.ts` (new): `runAsSpender({ agencyId, clientId?, flow }, fn)` and `currentSpender()` over `AsyncLocalStorage` (Node runtime — no route in `src/app` declares `edge`). `flow` is the feature (`generation | rewrite | editor | onboarding | analytics | sources | style_memo | brief`), set once at the boundary; it is the telemetry's "what" while the model is the "how much".
- `src/lib/billing/usage.ts` (new): `consumeUsage(entitlement, kind, cost)` → `{ allowed, used, quota }` over `consume_usage` via the admin client (the admin-client RPC pattern the retired image counter used), `refundUsage`, `readUsage(agencyId, periodKey)`; `AllowanceError` (typed: kind, used, quota, resetsOn); `allowanceResponse(err)` → 402 JSON for routes; the 80 % notification once per period (message carries the period key, `cooldownDays: 31`, through `notify` in `src/lib/notifications/notify.ts:61`).
- `src/lib/billing/telemetry.ts` (new): `recordAiUsage({ provider, model, tokens…, cost })` → `add_ai_usage` for `currentSpender()`; never throws, warns on failure. `src/lib/billing/ai-prices.ts` (new): the providers' list prices per million tokens per Claude model (input, output, cache read, cache write, web search per 1k), per fal model, per Tavily query, and the one USD→EUR rate that turns them into euro cents — every stored and displayed amount is euro.
- `callAnthropic` (`src/utils/ai-client.ts:84`) records the final message's `usage` for the current spender; with no spender in scope it throws — fail closed — except when the boundary marked itself `flow: 'brief'` (the global weekly brief, `src/ai/intelligence/generate-briefing.ts` calls `anthropic.messages.create` directly at its own site and records with a null agency). `queryTavily` (`src/lib/sources/tavily-client.ts:29`) records one query.
- `src/lib/visual/fal.ts:31-45`: `subscribeFal` reads `currentSpender()`, consumes 1 image credit **before** `fal.subscribe` for `FAL_MODEL`, `EDIT_MODEL`, `VECTOR_MODEL` (refund on throw; `DIS_MODEL` cutouts free), and records telemetry after. No spender in scope → refused. Signatures of `generateSlideImage`, `generateVectorAsset`, `editImageWithMask` are unchanged; this is what finally counts inpaint and vector calls, which record nothing today.
- `generateVisual` (`src/lib/visual/generate-visual.ts:56`) loses its `spender` parameter (identity now comes from the context, one way); its callers migrate in the same change: `src/app/api/ai/generate-visual/route.ts:73`, `src/app/api/ai/generate-background/route.ts:99`, `src/lib/visual/generate-post-visual.ts:66`. `image-spend.ts` and its after-the-fact `recordImageSpend` are gone (done).
- Boundaries that wrap `runAsSpender`: every gated route in step 6, `refreshStyleMemo` (`src/features/clients/actions/style-memo-actions.ts:16`), the generate cron's per-client loop (`src/app/api/cron/generate/route.ts:140`), the visuals cron's per-job call (`src/app/api/cron/visuals/route.ts:154`), and the analytics narrative (`resolveNarrative`, `src/features/analytics/lib/shared/narrative-shared.ts:153`, via `generateAnalyticsSummary`) — observed run decides whether the context survives `unstable_cache` or the spender is passed explicitly there.
- `generatePostVisual` (`src/lib/visual/generate-post-visual.ts:26`) adds `{ ok: false, reason: 'images_allowance' }` to its result union so the cron and the visuals route treat a refusal as a skip, not a failure.
- Rewrite counter: `src/app/api/ai/rewrite/route.ts` consumes 1 `rewrite` after ownership and before the model call; 402 on refusal.
- Consumption needs the entitlement: routes get it from `getCachedEntitlement(auth.agencyId)`; the visuals cron resolves it per client from the roster read in step 5.

→ verify: unit tests with a mocked fal client — consume before subscribe, refund on throw, 0 for `DIS_MODEL`, refused with no spender in scope; `callAnthropic` records `usage` for the spender and throws without one (mocked SDK); `generateVisual` refuses without calling fal when the RPC says no; inpaint, SVG and rewrite routes return 402 at quota; an observed run of one generation shows one `ai_usage_daily` row per (model, flow) with token sums that match the SDK's `usage`; the analytics page's narrative attributes to the agency (or the explicit-spender fallback is applied); `npm run writers` sees the retired image counter gone and `usage.ts` / `telemetry.ts` listed.

### 4. Drafts pool at the run, and the wire caps that make it honest
- `startGenerationRun` (`src/lib/generation/runs.ts:51`) takes `entitlement` and reserves `targetCount` drafts **before** the insert; a refusal returns a new claim `{ runId: null, refused: 'allowance' | 'locked', used, quota, resetsOn }` that both callers must honour — the existing "generation proceeds without a run row" path (`runs.ts:79-82`) must not apply to a refusal, and a reservation whose insert then fails is refunded inside the function. `finishGenerationRun` (`:90`) takes `{ entitlement, produced }` and refunds `target_count − produced` so a run that found fewer topics is charged only for what it wrote (the cron passes posts saved; the wizard passes drafts streamed).
- Callers: `src/app/api/cron/generate/route.ts:172` (entitlement from the widened context, step 5; refusal → `results.skipped_over_allowance` + `notify` type `allowance_reached`) and `src/app/api/ai/generate-stream/route.ts:84` (402 before the stream opens, before any research or Tavily call).
- Server-side maxima equal to what the UI already offers: `scheduleInputSchema.frequency_value` and `posts_per_week` `.max(7)` (`src/features/clients/schemas.ts:45-56`), `generateStreamSchema.targetPostCount` `.max(7)` and `priorityPosts` `.max(7)` (`src/features/generate/schemas.ts:110-118`), and the cron clamps `default_carousel_slides` to `MIN_CAROUSEL_SLIDES..MAX_CAROUSEL_SLIDES` (`src/app/api/cron/generate/route.ts:163`, constants at `src/utils/constants.ts:17-19`) the way the wizard schema already does.

→ verify: `startGenerationRun` returns the refusal without inserting when the RPC refuses; two concurrent runs against a 3-draft remainder open exactly one run (RPC atomicity, against local Postgres); a cron tick lists a locked agency's client under `skipped_unentitled` with no `generation_runs` row; zod rejects `targetPostCount: 8` and `frequency_value: 8`; a run with `target_count 3` that produced 1 leaves `used` at 1.

### 5. Cron gates — filter before the LIMIT, never after
- generate: `fetchScheduleContext` (`src/app/api/cron/generate/helpers.ts:28-76`) widens its `agencies` select from `id, timezone, mode` to the billing columns and exposes `entitlements: Map<agencyId, Entitlement>`; the route drops clients whose agency cannot spend inside the `dueClients` flatMap (`src/app/api/cron/generate/route.ts:127-138`), before the slot claim, and records them in `results.skipped_unentitled`.
- visuals: `src/app/api/cron/visuals/route.ts:62-77` adds `.in('client_id', [...entitled])` to the backlog query so paused workspaces never occupy the 100-row window (`BACKLOG_FETCH_LIMIT`) or the 12 image slots; then drops posts whose agency has fewer images left than `totalVisualSlots(post)` before `pickVisualBacklog` (`:95`), so a carousel is painted whole or not at all. An `images_allowance` refusal from `generatePostVisual` is counted as `skipped_allowance`. The attempt counter (`countAttempt`, `:115`) runs after the pre-filter, so a refusal costs an attempt only on a same-minute race with a wizard user — the CAS is the backstop, not the gate.
- publish: `publishDuePosts` (`src/features/publishing/lib/scheduler.ts:93`) adds `.in('posts.client_id', [...entitled])` to the due query (`:168-222`, the embed is `posts!inner` and `.lte('posts.scheduled_at', …)` already filters on it) and to the stranded sweep (`:112-155`), with need `'publish'` — rows of a paused workspace stay `scheduled`, unclaimed, and are not failed.
- metrics + comments: `syncRoster` (`src/features/analytics/lib/shared/sync-shared.ts:67-100`) and `syncAllClientComments` (`src/features/comments/lib/sync-comments.ts:81-110`) filter `connections` by the set (need `'publish'`) and count the dropped ones in `outcome.skipped`.
- refresh-tokens (`src/features/publishing/lib/refresh-tokens.ts:48`): NOT gated — one free Meta call keeps a paused workspace's token alive so reactivation needs no reconnect; said in its doc.
- `src/app/api/cron/__tests__/cron-invariants.test.ts` gains a describe: every cron route except `refresh-tokens` (and `billing`, step 8) reaches `@/lib/billing/entitled-clients` either directly or through its first-level `@/features/…`/`@/lib/…` import (the existing test only reads files under `src/app/api/cron`, so the new block resolves those imports one level).

→ verify: fixture tests — a locked agency's posts never reach `pickVisualBacklog`; `publishDuePosts` leaves its publication `scheduled` with `publish_attempts` unchanged; `syncRoster` counts the filtered connections; cron-invariants fails when the import is removed from any of the five spending crons.

### 6. Human gates, the brand cap, and the guard that keeps them
- `src/lib/billing/require-entitled.ts` (new): `requireEntitledRoute(agencyId, need)` → `NextResponse | null` (the shape `aiRateLimitResponse` returns, `src/lib/auth/rate-limit.ts:76`) and `requireEntitledAction(agencyId, need)` → `ActionResult` failure or `null`; both over `getCachedEntitlement`. Not attached to the auth funnels: read paths stay open so a lapsed customer keeps their data readable.
- One line after the existing auth/limiter at each cost-bearing site — need `'spend'`: the thirteen routes that call `aiRateLimitResponse`/`visualsRateLimitResponse` — `src/app/api/ai/analyze-url/route.ts`, `src/app/api/ai/detect-slop/route.ts`, `src/app/api/ai/generate-background/route.ts`, `src/app/api/ai/generate-stream/route.ts`, `src/app/api/ai/generate-svg/route.ts`, `src/app/api/ai/generate-visual/route.ts`, `src/app/api/ai/inpaint/route.ts`, `src/app/api/ai/isolate-subject/route.ts`, `src/app/api/ai/paste-from-url/route.ts`, `src/app/api/ai/rewrite/route.ts`, `src/app/api/ai/suggest-sources/route.ts`, `src/app/api/clients/[id]/brand-profile/reanalyze/route.ts`, `src/app/api/posts/[id]/visuals/route.ts` — and `src/features/clients/actions/style-memo-actions.ts`; need `'publish'`: `src/app/api/posts/[id]/publish/route.ts:32-37` and `schedulePosts` (`src/lib/actions/post-actions.ts:432`); need `'create'`: `createClient`.
- Brand cap: `createClient` (`src/features/clients/actions/client-actions.ts:44`) counts `clients` for the agency and refuses at `entitlement.limits.brands`; `provisionClient` (`src/features/clients/lib/provision-client.ts:42`, whose only caller is `createClient`) now receives `createAdminSupabaseClient()`, and a one-line migration `supabase/migrations/20260853_clients_insert_admin_only.sql` (new) — `REVOKE INSERT ON public.clients FROM anon, authenticated` — ships with this step, so a member can no longer insert a brand past the cap through PostgREST. No seat cap: `POST /api/settings/team/invite` is untouched.
- `src/lib/billing/__tests__/gate-coverage.test.ts` (new): an explicit allowlist `file → need`; asserts each listed file contains the matching `requireEntitled*` call, and that every file importing `@/lib/auth/rate-limit` is in the allowlist — a new rate-limited AI route cannot be added without a gate.

→ verify: route tests — each gated route returns 402 `{ code: 'locked' }` for a fixture agency past its grace and passes for an active one; `createClient` refuses the 2nd brand on starter and the 4th on trial; gate-coverage fails when a listed call is removed.

### 7. Settings › Plan, the shell, and the refusal states (inside the dashboard — no mock gate)
Design direction: `docs/redesign-mocks/direction-01.html` (paper ground, one forest brand tone, hairlines instead of card borders, one button system, micro motion only inside the app — hovers, focus, entrance; route changes never animate) as codified in `DESIGN.md`. Concretely for every new piece here: status only in the fixed pairs (Amber Background on Amber for attention, Clay Background on Clay for errors — `PILL_TONES.warn` / `.bad` in `src/components/ui/status-pill.tsx:10`), never lime for a plan name or any permanent property; one primary `Button` per page; prices and counts in `tabular-nums` at closed-ramp roles; `FormSection` for the Settings panel, `Card` for the wall, `SectionHeading` where a section opens; hover shifts tone not hue; no new tokens, no arbitrary values, no inline styles beyond a computed meter width; every new stateful client component gets a `.test.tsx` in the same change.
- `PlanSection` (`src/features/settings/components/plan-section.tsx:40`) rewritten from `entitlement` + `readUsage` (done): plan name in a neutral pill (the old lime `mark` pill broke DESIGN.md's fill-only-lime rule), status in Amber/Clay only, "Trial ends" / "Renews on" / "Ends on", meters for brands · drafts · images · rewrites in `tabular-nums` (Amber at ≥ 80 %). `UpgradeRailAction` (`:113`) and "No card on file" go; the Choose plan / Manage billing buttons land in Phase 2. `src/app/(dashboard)/settings/page.tsx:20-24` passes the entitlement and usage instead of `plan_client_limit`; the header meta in `src/features/settings/components/settings-view.tsx:108` reads the plan label from `PLANS`.
- `src/app/(dashboard)/layout.tsx:93-111` computes the entitlement from the `agencyData` it already holds and hands `{ state, trialEndsAt, limits, usage }` to `ShellProvider` (`src/components/layout/shell-context.tsx:234`); `src/components/layout/billing-banner.tsx` (new) — Amber when the trial ends in ≤ 3 days, at ≥ 80 % of an allowance, on past_due, on cancel_at_period_end; Clay for trial_grace and locked; `src/components/layout/billing-wall.tsx` (new) — read-only wall for `locked` with the page's one primary button, letting `/settings` through.
- `src/app/(generate)/layout.tsx` and `src/app/(onboarding)/layout.tsx` check only `requireAuthUserId` today; both add `getCachedUserRecord` + `getCachedEntitlement` and redirect to `/settings?tab=account&reason=<state>` unless `canSpend` / `canCreate`.
- Wizard and editor: `src/features/generate/hooks/use-draft-visuals.ts` treats a 402 as an allowance state (no retry); `src/features/generate/components/generate-flow.tsx` shows the 402 body's message; `src/features/canvas-editor/components/busy-hint.tsx` shows the allowance sentence; `src/features/calendar/components/queue-rail.tsx` shows a "Not publishing — workspace paused" pill for `locked`. Copy lives in `src/lib/billing/copy.ts` (new) so the Bulgarian strings are a one-file job later. Unit word on user-facing surfaces: "brand".

→ verify: component tests (`*.test.tsx`, role + accessible name) for `PlanSection` in every state and both modes, the banner and the wall; route tests for the two layout redirects; manual browser matrix trial → ending → grace → locked, desktop and mobile, both modes (jsdom cannot see layout — CLAUDE.md).

### 8. Billing cron — trial reminders (daily, spends nothing)
- `src/app/api/cron/billing/route.ts` (new), seventh entry in `vercel.json`, same `CRON_SECRET` bearer check as the other six: "trial ends in 3 days" (computed from `trial_ends_at`, never from `auth.users.created_at`), "trial ended today", and "workspace paused" at grace end — each recorded through `notify` with a dated message so redelivery is idempotent, and e-mailed to the agency's admin users.
- E-mail: one `sendEmail` in `src/lib/email/resend.ts` extracted from `sendApprovalEmail` (`:43`, today the only sender — one send path, not two); templates in `src/lib/email/templates.ts` with file snapshots in `src/lib/email/__tests__/templates.test.ts` (never hand-edited).
- Phase 2 adds the daily Stripe quantity reconcile here.

→ verify: snapshot tests for the templates; the selector picks agencies whose `trial_ends_at` is in `(now, now+3d]` once and never twice, and never one with a `stripe_subscription_id`; cron-invariants lists the new route as the second exception with its reason.

### 9. Phase 2 — Stripe in test mode: checkout, portal, webhook, quantity sync

- **Dependency and client.** `stripe` (^22) in `package.json`; `src/lib/billing/stripe.ts` (new): `stripeClient()` — one lazily built `Stripe` on `STRIPE_SECRET_KEY`, throwing without it, on the API version the installed SDK pins (its CHANGELOG names it; the Dashboard webhook endpoint is set to the same version, step 12 — since Basil a subscription's period lives on its items, an invoice's subscription under `parent.subscription_details`, its payments under `payments`, its tax under `total_taxes`, a line's price under `pricing`); the price id from `STRIPE_PRICE_ID` (one env var per environment; the price is made by hand in the Dashboard, below) — no lookup call at runtime; and since there is one plan, the webhook writes `plan = 'pro'` for any subscription it sees. `src/lib/billing/checkout.ts` (new) holds the two Stripe calls a person can trigger — `createCheckoutSession`, `createPortalSession` — so `stripe` is imported only under `src/lib/billing/` and by the webhook route, as the Shape section says; the settings actions call these and never the SDK.
- **The catalogue, by hand.** One product "Kontuur" with one price — €19, `eur`, monthly, per unit, unit label "client" (the app's word, so an invoice line reads "3 × client"), tax behaviour exclusive, tax code `txcd_10103001` — created in the Dashboard in test mode and again in live mode (step 12), each id in that environment's `STRIPE_PRICE_ID`. No script: it is one product and one price, once per account, and a script for it would bring `tsx` into the repo and a second place stating what `PLANS` (`src/lib/billing/plans.ts:41`) already says. A price change is a new price and a migration, out of scope. The customer portal, also by hand (step 12): card update on, name / email / address / tax-id update on, cancel at period end on, **subscription update off** (the quantity follows the client count from the app, never from the portal), **invoice history off** (Stripe's PDFs carry `PREFIX-NNNN` numbers that are not the legal ones — ours are in Settings).
- **One plan in the code that knew two.** `src/lib/billing/plans.ts`: `PlanId` (`:18`) becomes `'trial' | 'pro' | 'house'`, `PLANS` (`:41`) becomes `PRO_PLAN = { priceCents: 1900, perBrand }` — `minimumBrands` and `maxBrands` existed to tell Starter from Agency and go with them — and `PLAN_LABELS` (`:69`) and the header comment follow; `src/lib/billing/entitlement.ts`: `isPlanId` (`:83`) names the three, and the paid branch (`:210-225`) reads `PRO_PLAN`, `brands = max(1, subscription_quantity)`, `brandsUnlimited: true`; `src/lib/billing/copy.ts`: `brandCapReached` (`:101`) loses "Move to Agency to add more." — on `pro` the cap is unbounded and `addBrandRefusal` (`:112`) never reaches it, so the sentence keeps only the trial case; tests that name a paid plan move to `pro`: `src/lib/billing/__tests__/entitlement.test.ts` (`:32`, `:103-117` — the Starter cap case becomes "quantity 4 → 4 brands"), `src/lib/billing/__tests__/copy.test.ts` (`:125-126`), `src/features/settings/components/__tests__/plan-section.test.tsx` (`:46`, `:65`), `src/lib/billing/__tests__/reminders.test.ts` (`:69`). The settings page and `PlanSection` read `PLAN_LABELS[entitlement.plan]` and need nothing. The database's CHECK narrows in step 10's migration.
- **The two actions** in `src/features/settings/actions/billing-actions.ts` (new), each admin-only: `resolveActionAuth` (`src/lib/auth/helpers.ts:239`) already reads `getCachedUserRecord`, whose row is `agency_id, role` (`USER_AUTH_COLUMNS`, `src/lib/queries/select-columns.ts:351`), and drops the role — it now returns it too (its callers destructure what they use, so none changes), and the two actions refuse a non-admin with one sentence from copy.ts; no second `users` read and no wrapper. `removeTeamMember` (`src/features/settings/actions/team-actions.ts:21`) keeps its fresh `verifyAdminRole` read, untouched. Neither action takes input, so there is no body to validate. Each is auth, the one rule, one call into `checkout.ts`:
  - `startCheckout()` → `ActionResult<{ url }>`: refused, with a sentence from copy.ts, when a live subscription (active, trialing or past due) already exists — the portal is the way then. The quantity is the workspace's client count, at least one (`countClientsByAgency`, `src/lib/queries/db.ts:70`). `ensureStripeCustomer(agencyId)` (`src/lib/billing/subscription-store.ts` (new) — the one writer of `stripe_customer_id`: `customers.create({ name, metadata: { agency_id }, test_clock })` with `test_clock` only when `STRIPE_TEST_CLOCK` is set — test mode, for the renewal tests — then the row) and `checkout.sessions.create` with `mode: 'subscription'`, `customer`, `line_items: [{ price, quantity: max(1, clients) }]`, `payment_method_types: ['card']` — cards only (Apple Pay and Google Pay ride on it): the alternative regime's condition is card payments and nothing else, so no SEPA debit and no bank transfer; `automatic_tax: { enabled: true }`; `billing_address_collection: 'required'`; `tax_id_collection: { enabled: true }` — not required, consumers are customers; `customer_update: { address: 'auto', name: 'auto' }` so renewals are taxed against the saved address; `consent_collection: { terms_of_service: 'required' }` with `custom_text.terms_of_service_acceptance.message = CHECKOUT_CONSENT` (step 12); `locale: 'auto'`; `subscription_data: { metadata: { agency_id } }` — no deferred `trial_end`: the first charge is immediate, so the subscription is `active` from its first event and the Stripe-`trialing` branch of `entitlementFor` (`src/lib/billing/entitlement.ts:194`) is never entered by this flow (it would have shown "choose a plan" to a customer with a card on file); a converter who wants the rest of the free days subscribes later — the banner asks only in the last three; `success_url` / `cancel_url` = `${resolveAppUrl()}${PLAN_AND_BILLING_PATH}&billing=success|cancelled`. Never provisions from the success URL.
  - `openBillingPortal()` → `billingPortal.sessions.create({ customer, return_url })`. There is no plan change: the only thing that moves the price is the client count, below.
- **Quantity follows the brand count.** `src/lib/billing/quantity-sync.ts` (new): `syncSubscriptionQuantity(agency, brandCount)` — on an active subscription; the quantity written is absolute — the client count, never ±1 — so any later change heals an earlier miss. An increase goes to Stripe with `proration_behavior: 'always_invoice'` and `payment_behavior: 'error_if_incomplete'` (a declined pro-rata charge answers 402 and leaves the subscription as it was, instead of applying the quantity and sliding it to past_due) BEFORE `provisionClient` runs in `createClient` (`src/features/clients/actions/client-actions.ts:52`) — no Stripe success, no brand, and the refusal carries Stripe's reason; if `provisionClient` then fails, the quantity is put back at once with `proration_behavior: 'create_prorations'`, which credits the charge. A decrease comes after the row is gone in `deleteClient` (`:144`) with `proration_behavior: 'none'`, never below one; a failure there is logged as an error, and the next change heals it. Idempotency key `randomUUID()` per call — it protects the SDK's own retries of one request, and a fresh key per request means 3→4→3→4 inside Stripe's 24-hour window never replays a stale answer. The "adding a client" promise from "What the user sees": `AddClientAction` (the existing button, `src/features/clients/components/`) shows `addBrandCost(entitlement)` from copy.ts — "Adds €19 a month, charged pro rata today" — on a paid workspace; the exact figure is on the invoice, no preview call from a button. No daily reconcile: it would be a second `agencies` read per tick for a drift the absolute write already heals.
- **The webhook.** `src/app/api/billing/webhook/route.ts` (new): raw `request.text()` + `stripe-signature` → `stripe.webhooks.constructEvent(…, STRIPE_WEBHOOK_SECRET)` (the raw-body-then-verify shape of `src/app/api/meta/data-deletion/route.ts:30`); `recordBillingEvent` and `finishBillingEvent` live in the route module (two five-line functions with one caller — no separate file): the insert into `billing_events` is the `ignoreDuplicates`-then-read-back shape of `src/features/publishing/lib/publication-store.ts:49` — an event whose row already carries `processed_at` answers 200 and does nothing; one whose row is there with `processed_at` null failed before and runs again, which is what a Stripe retry after a 500 needs; two concurrent deliveries may both run, and every write below is idempotent, so that is harmless. Subscribed events, six: `customer.subscription.created|updated|deleted`, `invoice.paid`, `invoice.payment_failed`, `credit_note.created` — the agency comes from `subscription.metadata.agency_id`, set at checkout (an invoice reaches its subscription through `parent.subscription_details`), so `checkout.session.completed` adds nothing and no customer-to-agency read exists; disputes stay in the Dashboard, where Stripe already emails them. For the subscription and invoice events the handler **re-fetches the subscription** (`subscriptions.retrieve(id)` — events are unordered, and the re-fetch is the current truth, so no ordering rule is needed; Stripe's own docs say `event.created` is not an order) and calls `applySubscriptionSnapshot` (`subscription-store.ts`, the ONE writer of the billing columns). Which subscription owns the row: the stored `stripe_subscription_id`, or the one a `customer.subscription.created` announces — a late event for any other (the cancelled one, after a re-subscription) is recorded and ignored. The snapshot writes `plan = 'pro'`, `subscription_status`, `stripe_subscription_id`, `subscription_quantity`, `cancel_at_period_end`, `billing_updated_at = now()`; **the period columns (from `items.data[0]`) are written when the stored `current_period_start` is null — the first fill, so a paying customer is never locked between `subscription.created` and `invoice.paid` (`entitlementFor` locks a paid row with no period, `src/lib/billing/entitlement.ts:212`) — and on `invoice.paid`, never on any other event**: a failed renewal advances Stripe's period but must not move the allowance bucket; `past_due_since` is set on `invoice.payment_failed` when null and cleared on `invoice.paid` — both invoice-driven writes unconditional and idempotent by invoice. `invoice.paid` with `amount_paid > 0` then issues the sale document (step 10) and schedules its delivery with `after()` (the pattern of `src/app/api/extract/start/route.ts:64`); `credit_note.created` issues the credit note only for `type === 'post_payment'` with `refunds.length > 0` — a pre-payment note moves no money, and a credit to the customer's balance is not a refund the audit file may report as one; `invoice.payment_failed` raises the `payment_failed` bell and email (step 12). `finishBillingEvent` stamps `processed_at` or `error`; a throw answers 500 so Stripe retries; `revalidateTag('agencies', 'max')` after every row write. `maxDuration = 60` for the render inside `after()`, and `/api/billing/webhook` joins `outputFileTracingIncludes` in `next.config.ts:68` (the Chromium binary is force-included per function; without it the render fails on Vercel with "…/@sparticuz/chromium/bin does not exist", the comment at `:65-67`). No boundary-validation entry: the route reads `request.text()`, and `READS_BODY` (`src/app/api/__tests__/boundary-validation.test.ts:23`) matches `request.json()` alone.
- **Settings wiring.** `PlanActions` (new client component, `src/features/settings/components/plan-actions.tsx`, with its `.test.tsx`) under `PlanSection` (`src/features/settings/components/plan-section.tsx:54`): on a trial, grace or paused workspace one primary "Choose plan" button with the sentence beside it — "€19 a month per client · N clients today" from copy.ts; on a paid one "Manage billing" (portal); each button calls its action and `window.location.assign(url)`. one reader of the return flag: `SettingsPage` reads `searchParams.billing` and hands it to `PlanActions`, which toasts, clears the param (`clearQueryParams`, `src/utils/url.ts:13`) and, on success, polls with the bounded `setTimeout` loop of `src/features/onboarding/hooks/use-extraction-status.ts:47` — `router.refresh()` every 2 s for up to 10 s while the plan is still `trial`; the Canva effect in `settings-view.tsx` (`:81`) is not extended. The page's read is uncached (`fetchAgencyById`, `src/lib/queries/db.ts:132`), so the webhook's row shows the moment it lands. Money in that sentence through `formatMoney` in `src/utils/format.ts` (new there: euro, two decimals; its readers are this sentence, the add-client sentence and the document).
- **Registries and env.** `docs/OPERATIONS.md` Account rows: "Create the Stripe customer | `ensureStripeCustomer`", "Write the subscription snapshot | `applySubscriptionSnapshot`", "Record / finish a Stripe event | `recordBillingEvent` / `finishBillingEvent`", "Keep the paid quantity equal to the brand count | `syncSubscriptionQuantity`"; `scripts/table-writers.json`: `agencies` += `src/lib/billing/subscription-store.ts` (new), `billing_events` → `src/app/api/billing/webhook/route.ts` (new). `.env.example`: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID`, `STRIPE_ACCOUNT_ID`, `NRA_ESHOP_NUMBER`; test keys on Preview, live on Production, never mixed; locally `stripe listen --forward-to localhost:3000/api/billing/webhook`. The Stripe account's business profile: ЧЕЛЛИНГ ООД / kontuur.app / descriptor `KONTUUR` (the account created on 2026-09-12 still shows aboutsocialmedia.io).

→ verify: `src/lib/billing/__tests__/subscription-store.test.ts` (new) — the snapshot maps a fixture subscription to the row, fills the period when the stored one is null, advances it only on `invoice.paid`, clears `past_due_since` on a paid invoice, ignores a subscription that is not the row's; `src/app/api/billing/__tests__/webhook.test.ts` (new) — bad signature → 400, a processed duplicate → 200 with one row, a retry of a failed event runs again, a processing throw → 500 with `error` set, a pre-payment credit note issues nothing; `billing-actions.test.ts` — the refusal on a live subscription, the refusal for a member, the quantity of a solo (1) and of a three-client agency (3); `quantity-sync.test.ts` — increase before provision with `error_if_incomplete`, the put-back after a failed provision, the absolute quantity, decrease never below 1; `PlanActions` component test per state; the migrated Phase 1 tests green with `pro`. End to end in test mode with `stripe listen`: signup → trial → Checkout with a German VAT ID (reverse charge) and with a Bulgarian address (20 %) → Settings shows Active within 10 s → add a client (quantity +1, a pro-rata invoice and its document) → delete one (quantity −1, nothing charged) → a customer created under a test clock (`STRIPE_TEST_CLOCK`), its card swapped to `4000 0000 0000 0341` in the portal, the clock advanced past the renewal → `invoice.payment_failed` → past_due banner, bell and email → card swapped back and the invoice retried → paid → cancel at period end → "Ends on" → locked. No live charge is taken in this plan.

### 10. Every payment leaves one document — the invoice that is also the Н-18 sale document

- **What the regulation asks, read from the text** (ДВ 8/2020 as quoted by news.inbalance.bg; the NRA's own XSD is vendored in step 11): a sale under the alternative regime is registered by a document carrying the seller's identity, a unique order number, the transaction reference, each item with its tax group, totals and payment method, and a QR code (чл. 52о ал. 1); **an invoice carrying items 3–6 may stand in for that document (ал. 3)** — so one document per payment, not two; it is delivered to the customer electronically at the time of payment (ал. 5). The QR encodes, in this order and separated by `*`: the NRA e-shop number, the order number, the transaction reference, `YYYY-MM-DD`, `HH:MM:SS`, the total (Приложение 18а). Bulgarian invoice numbers are ten digits, ascending, without gaps (ППЗДДС чл. 78) — Stripe's are `PREFIX-NNNN`, so the number is ours.
- **Migration `supabase/migrations/20260855_billing_documents.sql` (new)**, applied before the code deploys (the lesson of step 1). First the one plan: a `DO` block raises if any row carries `plan in ('starter', 'agency')` (none does — nothing has been sold), then `agencies_plan_check` (last re-created in `supabase/migrations/20260853_billing_followups.sql`) is dropped and re-added as `plan in ('trial', 'pro', 'house')`. Then the additive part: `document_counters (series text primary key, last bigint not null)` seeded `('documents', <range start − 1>)` — the range start is the number range the accountant assigns to Kontuur's documents, a named constant at the top of the migration: ППЗДДС чл. 78 lets one company run several ranges as long as every number stays unique, and ЧЕЛЛИНГ ООД may already issue invoices from its accounting software, so Kontuur's numbers must not collide with those; one series for invoices and credit notes (that notes share the invoices' sequence is the accountant's fourth confirmation); `sale_documents (id uuid primary key, number bigint not null unique, kind text check in ('invoice','credit_note'), agency_id uuid references agencies on delete set null, stripe_invoice_id text, stripe_credit_note_id text, stripe_charge_id text, stripe_refund_id text, refunds uuid references sale_documents, issued_at timestamptz not null, customer jsonb not null, lines jsonb not null, net_cents bigint not null, vat_cents bigint not null, gross_cents bigint not null, vat_rate integer not null, vat_basis text check in ('domestic','oss','reverse_charge','outside_eu'), storage_path text, delivered_at timestamptz, delivery_error text, created_at timestamptz default now())`, with unique partial indexes on `stripe_invoice_id where kind = 'invoice'` and on `stripe_credit_note_id`; **`on delete set null`, not cascade** — a deleted workspace's documents are kept ten years (ДОПК). RLS on both; `sale_documents_admin_read` is the predicate of `billing_events_admin_read` (`supabase/migrations/20260853_billing_followups.sql:38`); `document_counters` gets no policy — a policy that cannot name the caller fails the guard (`IDENTIFIES_CALLER`, `src/app/__tests__/rls-policies.test.ts:28`) — and joins `POLICYLESS` (`:55`) with the reason "read and written by the service role alone, through `issue_sale_document`"; INSERT, UPDATE and DELETE revoked from the tenant on both. RPC `issue_sale_document(p jsonb) returns sale_documents`, SECURITY DEFINER, EXECUTE for the service role only: returns the existing row for the same `kind` and Stripe id when there is one, else `update document_counters set last = last + 1 … returning last` and inserts — one transaction, so a failed insert never burns a number and two webhook deliveries never mint two. The private bucket: `insert into storage.buckets (id, name, public) values ('billing-documents', 'billing-documents', false) on conflict do nothing` — no storage policy, so only the service role reads it. Then `npm run db:types`.
- **`src/lib/billing/documents.ts` (new).** `issueSaleDocument(admin, invoice)`: from the invoice re-fetched with `expand: ['total_taxes.tax_rate_details.tax_rate', 'payments.data.payment.payment_intent']` — the customer snapshot (`customer_name`, `customer_email`, `customer_address`, `customer_tax_ids`), the lines (description, quantity, unit net, net, the line's `period`), the tax from `total_taxes[0]`: its amount, and from the expanded tax rate its `percentage` and `country` — `vat_basis` is `domestic` when that country is BG (which is also what the "small seller" option charges an EU consumer until an OSS registration exists, so the address alone would mislabel it), `oss` when it is another EU country, `reverse_charge` when `taxability_reason` is `reverse_charge`, `outside_eu` when no tax was collected and the customer's country is outside the EU (`EU_COUNTRIES`, a constant local to the file); `not_collecting` with an EU address (VAT was due and not charged — a registration gap, not a document to issue), any other reason, or a currency other than `eur`, throws — the event fails, Stripe retries, nothing half-issued is written; the charge id (`latest_charge` on the expanded payment intent), `issued_at = status_transitions.paid_at`; `amount_paid === 0` → no document. `issueCreditNote(admin, creditNote)`: the invoice document it refunds, found by `credit_note.invoice`, the note's amounts, `refunds[0].refund` as `stripe_refund_id`, `kind: 'credit_note'`. Both call the RPC and are the only writers (hand-listed in `scripts/table-writers.json` under the `[hand-listed]` rule, `scripts/table-writers.mjs:105`); the RPC's input is typed as `TablesInsert<'sale_documents'>` (`src/types/database.ts:1840`), never a hand-written interface — three column names in one is a mirror to `src/types/__tests__/row-mirrors.test.ts`. Nothing is written back to Stripe: the row carries the charge id, which is what a Stripe payout report names, so the accountant ties a payout line to our number through our table. **Refunds go through Stripe credit notes only** (Dashboard → invoice → credit note with refund): that is the one operation, and the runbook says so — a refund made any other way produces no document and no audit-file line, which is the founder's to avoid, not the code's to guess at.
- **`deliverSaleDocument(admin, id)`** (same file): `renderSaleDocumentHtml(row)` → `renderPdf(html)` → `admin.storage.from(BILLING_DOCUMENTS_BUCKET).upload('<agencyId>/<number>.pdf', pdf, { contentType: 'application/pdf', upsert: true })` → `sendEmail({ to, content: documentEmail(row, planUrl), attachments: [{ filename, content: pdf }], idempotencyKey: row.id })` — `to` is the invoice's `customer_email` (Checkout always collects one; a row without it is a `delivery_error`) → `delivered_at`; any failure lands in `delivery_error` and the row stays undelivered for the daily retry (step 11). Called from the webhook inside `after()` and from the cron; idempotent by construction: the upload is an upsert, a delivered row is never re-sent, and the Resend idempotency key (kept 24 h) means an email that went out before a lost `delivered_at` write is not sent twice by the retry. `uploadToBucket` (`src/features/assets/lib/storage.ts:22`) is not reused: it is private and returns a public URL, which a private bucket has no use for.
- **`src/lib/billing/document-render.ts` (new)** — `renderSaleDocumentHtml(row)`, async (the QR encoder returns a promise) and free of I/O: bilingual labels (Фактура / Invoice, Кредитно известие / Credit note); the seller block from `COMPANY` in `src/utils/constants.ts` (new there: legal name ЧЕЛЛИНГ ООД / Chelling Ltd, ЕИК 206770508, VAT BG206770508, address — the two legal pages spell the same identity by hand, `src/app/(marketing)/terms/page.tsx:153` and `src/app/(marketing)/privacy/page.tsx:217`, noted as duplication and left for the Phase 3 rewrite); `NRA_ESHOP_NUMBER` and `STRIPE_ACCOUNT_ID` read from env inside this file and **refused when unset** — a QR without the shop number is a wrong document; `TAX_GROUPS` beside them — 'Б' for 20 % Bulgarian VAT, 'А' for reverse charge, outside the EU and OSS, **to be confirmed by the accountant before the first live sale**; the customer block with its VAT number or ЕИК; the order number (the Stripe invoice id); the transaction reference (the charge id); the lines with tax group, net · VAT (the rate and its basis: "20 %", "обратно начисляване — чл. 21, ал. 2 ЗДДС", "извън обхвата — чл. 21 ЗДДС", or the customer country's OSS rate) · gross, in EUR; the QR as inline SVG from `qrcode` (`toString(payload, { type: 'svg' })`; `qrcode` ^1.5 with `@types/qrcode`, MIT, pure JS) with the payload built by `qrPayload(row, eShopNumber)` — exported so a test pins the exact string; the QR's date from `toDateKey(date, 'Europe/Sofia')` (`src/utils/date-helpers.ts:43`) and its time from `getZonedParts` (`:55`), which gains `second` beside hour and minute; the printed date through `formatLongDate` (`src/utils/format.ts:16`); money through `formatMoney` (step 9). Tables and inline styles, the discipline of `src/lib/email/layout.ts:85`, because Chromium prints it. **`src/lib/render/pdf.ts` (new)** — `renderPdf(html)`: `getBrowser()` (`src/lib/render/browser.ts:29`) → `page.setContent` → `page.pdf({ format: 'A4', printBackground: true })` → Buffer; the page closed in `finally`. Every function that renders lives in a route listed in `next.config.ts` `outputFileTracingIncludes`: the webhook (step 9) and the billing cron (step 11).
- **Email.** `sendEmail` (`src/lib/email/resend.ts:39`) gains `attachments?: Array<{ filename: string; content: Buffer }>` (Resend's `attachments[].content` takes a Buffer; the 40 MB cap is far above a two-page PDF) and `idempotencyKey?: string` (the SDK's send option) — both optional, so its two callers (`src/features/approval-portal/lib/send-for-approval.ts:96`, `src/lib/billing/reminders.ts:141`) and the mock in `reminders.test.ts` are untouched; `documentEmail(row, planUrl)` in `src/lib/email/templates.ts` — "Your invoice from Kontuur" / "Your credit note from Kontuur", the number, the amount, one paragraph, a CTA to Plan & billing; a file snapshot beside the three reminders. In the Stripe Dashboard the founder switches Stripe's own receipt and invoice emails off (step 12), so a customer gets one document — ours.
- **Reads and the download door.** `SALE_DOCUMENT_COLUMNS` in `src/lib/queries/select-columns.ts`; `fetchSaleDocumentsByAgency(agencyId)` in `src/lib/queries/db.ts` (admin client, newest first, the `unwrap` shape of `fetchTeamMembersByAgency`, `:162`); `BillingDocuments` (new server component, `src/features/settings/components/billing-documents.tsx`) under `PlanSection` on the Account tab, rendered by the settings page only for an admin (`isAdmin`, `src/app/(dashboard)/settings/page.tsx:43`): number · date · kind · amount in `tabular-nums` · "Download" — a signed URL per row, minted at render on the admin storage client (`createSignedUrl`, one hour), the rows already scoped to the agency by the read; no route and no second ownership check. `BILLING_DOCUMENTS_BUCKET` joins the two bucket names in `src/utils/constants.ts:50`.
- **Registries.** `docs/OPERATIONS.md` gains an "Invoices" section: "Issue the invoice for a paid Stripe invoice — also the Н-18 sale document | `issueSaleDocument`", "Issue a credit note for a Stripe credit note | `issueCreditNote`", "Deliver a document — PDF to the bucket, email to the customer | `deliverSaleDocument`"; `scripts/table-writers.json`: `sale_documents` → `src/lib/billing/documents.ts` (new; hand-listed, because the write is an RPC).

→ verify: `documents.test.ts` — the mapping for four fixture invoices (a Bulgarian company at 20 %, a German company on reverse charge, a German consumer at 19 % OSS, a Skopje company outside the EU), the €0 invoice that yields no document, `not_collecting` with a German address throwing, a second call for the same invoice returning the same number (the RPC mocked to return the existing row), a credit note pointing at its invoice with its refund id, a pre-payment note and a balance credit issuing nothing; `document-render.test.ts` — the QR payload is exactly `<shop>*<order>*<txn>*<YYYY-MM-DD>*<HH:MM:SS>*<total>` for a fixture, two HTML snapshots (invoice, credit note), every чл. 52о item present by text; `BillingDocuments` — rendered for an admin only, one signed URL per row; observed run: `stripe listen` → a paid test invoice → a PDF in the bucket, one email with the PDF attached, the row's `delivered_at` set.

### 11. The month's audit file, the founder's runbook, and the daily backstops

- **`src/lib/billing/audit-file.ts` (new)** — `buildAuditFile({ seller, month, documents, creditNotes })`, pure → the XML that `dec_audit.xsd` describes (the NRA's file of 06.06.2022, vendored with its sample as `docs/n18/dec_audit.xsd` and `docs/n18/vik_simple.xml`): the header `eik`, `e_shop_n`, `domain_name`, `e_shop_type` 1, `creation_date`, `mon`, `god`; one `orderenum` per invoice document paid that month — `ord_n` (the Stripe invoice id), `ord_d`, `doc_n` (our number), `doc_date`, `artenum[]` (`art_name`, `art_quant`, `art_price` net unit, `art_vat_rate`, `art_vat`, `art_sum`), `ord_total1` net, `ord_disc` 0, `ord_vat`, `ord_total2` gross, `paym` **2** (virtual POS — the accountant confirms 2 against 4, "payment service provider"), `pos_n` (the Stripe account id), `trans_n` (the charge id), `proc_id` ("Stripe Technology Europe Limited"); one `rorderenum` per credit note issued that month — `r_ord_n`, `r_amount`, `r_date`, `r_paym` 2 (by card) — with `r_ord` and `r_total`. Amounts as two-decimal strings that sum exactly (the builder asserts lines = totals and throws otherwise, so a bad file cannot leave the machine); text values through `escapeHtml` (`src/lib/email/layout.ts:44` — the five XML entities exactly, so no second escaper); the prolog says `encoding="windows-1251"` as the sample does and the content is kept ASCII (English line names, no Cyrillic) so the declaration is true. The XSD's field notes still say "в лв." (2022): euro amounts go in as they are, and the portal's own validator on the first upload is the check — if the NRA has republished the schema for euro, the builder follows it, one file.
- **The download door for the file** — `src/app/api/billing/audit-file/route.ts` (new): `GET ?month=YYYY-MM`, guarded by `unauthorizedCron` (`src/lib/cron/authorize-cron.ts:8` — the one bearer check; the founder holds `CRON_SECRET`, nobody else), `month` through zod, the month's rows via `fetchSaleDocumentsForMonth` in `src/lib/queries/db.ts` (admin client), the XML answered as `application/xml` with a `Content-Disposition` filename. A route rather than a script because the admin client is `server-only` (`src/lib/supabase/admin.ts:1`), which does not resolve outside Next, and an ad-hoc client is what `docs/CLAUDE.md` forbids. The runbook line: `curl -H "Authorization: Bearer $CRON_SECRET" "https://kontuur.app/api/billing/audit-file?month=2026-10" -o audit-2026-10.xml`; the founder uploads it at inetdec.nra.bg with a КЕП by the 15th; a month with no paid document answers 204 and produces no file (the accountant confirms nothing is due then). Outside `src/app/api/cron`, so `cron-invariants` never sees it.
- **The billing cron grows one backstop** (`src/app/api/cron/billing/route.ts:13`): `retryUndeliveredDocuments(admin)` in `documents.ts` — every `sale_documents` row with `delivered_at` null and older than ten minutes goes back through `deliverSaleDocument`. It reports into the run summary the route already logs; `remindTrialWorkspaces` (`src/lib/billing/reminders.ts:80`) keeps its behaviour (step 12 only extracts its sender). `/api/cron/billing` joins `outputFileTracingIncludes` in `next.config.ts:68` for the retry's render.
- **Docs.** `docs/n18/README.md` (new): the Приложение 33 data as the founder will type it (domain kontuur.app; hosting Vercel, Dublin; software: own, this repository; database Supabase, EU; payment provider Stripe Technology Europe Limited, IE3206488LH; the accounts money lands on — the Stripe balance and the company's bank IBAN; the virtual POS identifier — the Stripe account id), the monthly runbook, and the accountant confirmations this step assumes.

→ verify: `audit-file.test.ts` — a fixture month with two invoices (20 % and reverse charge) and one credit note renders to a file snapshot; the totals assertion throws on a mismatched fixture; the payment codes and the ASCII rule; the audit-file route — 401 without the bearer, 400 on a bad month, 204 on an empty one; `documents.test.ts` gains the retry (an undelivered row is delivered once, a delivered one untouched); an observed download against the test-mode rows, and the file accepted by the NRA portal's validator on the first real upload.

### 12. The consumer side, the two states the shell does not know yet, and the hand work

- **Consent at checkout.** `CHECKOUT_CONSENT` in `src/lib/billing/copy.ts`: one sentence in English and one in Bulgarian — the customer asks for the service to start now and understands the 14-day right of withdrawal is then kept pro rata (ЗЗП чл. 49 ал. 9, чл. 55) — passed as `custom_text.terms_of_service_acceptance.message` beside `consent_collection.terms_of_service: 'required'` (step 9); the Terms link is the existing `/terms`. The final words are the lawyer's; the plumbing is this step's.
- **Cancel at period end reaches the shell.** `Entitlement` (`src/lib/billing/entitlement.ts:49`) gains `endsOn: Date | null` — `current_period_end` while `cancel_at_period_end` — set in every literal builder: `noEntitlement` (`:111`) and the four inside `entitlementFor` (`:131`); `shellNotice` (`src/lib/billing/copy.ts:130`) widens its `Pick` to `endsOn` and gains the Amber case "Your plan ends on … — renew in Plan & billing."; `nextDate` in `plan-section.tsx` (`:44`) shows "Ends on" first; the hand-built entitlement literals in `src/lib/billing/__tests__/copy.test.ts` take the field (the other fixture files cast and need nothing); `entitlement.test.ts` and `copy.test.ts` gain the cases.
- **A failed renewal is told.** `BillingReminderType` (`src/types/api.ts:307`) widens to `payment_failed` — one type, not a parallel one — and `ReminderOutcome.notified` (`src/lib/billing/reminders.ts:62`, initialised at `:92`) gains the fourth zero while the cron's log line (`src/app/api/cron/billing/route.ts:22`) keeps printing its three; `REMINDER_COPY` (`src/lib/billing/copy.ts:181`) gains the `payment_failed` subject, label, headline and detail ("Update your card in Plan & billing …"); the webhook's `invoice.payment_failed` branch calls `notify` (`src/lib/notifications/notify.ts:66`) with the dated `shellNotice` sentence and, behind `'written'`, `reminderEmail('payment_failed', …)` (`src/lib/email/templates.ts:56`) to the admins — the same pair `remindTrialWorkspaces` uses, extracted into `remindWorkspace(admin, { agencyId, type, message, to })` in `src/lib/billing/reminders.ts`: it takes the admin emails, so the cron keeps its one batched `users` read per tick and the webhook resolves its one workspace's admins through `fetchTeamMembersByAgency` (`src/lib/queries/db.ts:162`) filtered to `role === 'admin'` — no new `.from('users')`. A snapshot for the new email.
- **Hand work, in order, before the first live sale** (the founder's; none of it is code): 1 Stripe Dashboard — business profile ЧЕЛЛИНГ ООД / kontuur.app / descriptor `KONTUUR`, the Terms of Service and Privacy URLs under Public business details (Checkout refuses `consent_collection` without a Terms URL), EUR payouts, Tax → Locations: Bulgaria domestic with the "small seller" answer *yes* until an OSS registration exists (then *no* plus a Union OSS registration), Stripe's receipt and invoice emails off, the customer portal configured as step 9 lists; 2 create the product and price by hand as step 9 lists, in test mode and again in live mode, and put each id in that environment's `STRIPE_PRICE_ID`; 3 NRA — Приложение 33 for kontuur.app from `docs/n18/README.md` (new), then `NRA_ESHOP_NUMBER` and `STRIPE_ACCOUNT_ID` in Vercel; 4 accountant — the four confirmations (invoice as the document, the payment code, the tax groups, the document number range and that credit notes share it) and the OSS registration once the first consumer outside Bulgaria appears (Stripe's threshold monitor emails when it is near); 5 `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` live values on Production only, and the webhook endpoint registered in live mode with the six event types of step 9, its API version set to the one the SDK pins.

→ verify: the entitlement and copy tests for `endsOn`; `reminders.test.ts` gains `remindWorkspace`; the `payment_failed` snapshot; in test mode the failing-card renewal produces one bell row, one email and the Amber banner, and cancelling at period end shows "Ends on" in Settings and the shell; the checkout page shows the consent sentence and refuses to pay without the tick.

### 13. Count what landed — the meter moves only for a picture, a draft or a rewrite that exists

- **Why.** The ledger reserves BEFORE the provider call (`consumeUsage`, `src/lib/billing/usage.ts:34`) and "not counted" is only ever a refund afterwards (`refundUsage` at `:71`, `releaseCharged` at `:128`, the catch in `subscribeFal` at `src/lib/visual/fal.ts:46`). A refund that never runs — the function killed at `maxDuration`, a refund RPC that errors and is only logged — leaves a failed generation on the customer's meter for the rest of the period. Observed 2026-09-19 in the test workspace: 16 fal calls, 15 pictures, 16 counted. The founder's verdict: charging for a failure reads as a scam. The reservation stays (it is what makes the cap atomic under the wizard's six lanes); what changes is that the counter the customer sees moves only when the thing lands.
- **Migrations `supabase/migrations/20260857_count_what_landed.sql` (new) and `supabase/migrations/20260858_age_the_reservation_reset.sql` (new)** — the second carries the two fixes the review produced after the first had reached production (`reserved_at` and the upsert settle) — applied before the code deploys. `usage_counters` (`supabase/migrations/20260852_billing_foundation.sql:84`) gains `pending integer not null default 0 check (pending >= 0)` and `reserved_at timestamptz`: `count` is what landed, `pending` what is in flight, `reserved_at` when the row was last reserved on. `consume_usage` (`:97`) is re-created to reserve into `pending` and stamp `reserved_at` — the compare-and-set becomes `count + pending + p_cost <= p_quota` and `used` in its answer is `count + pending`, the committed figure a refusal sentence and the cron budgets need. `refund_usage` (`:127`) is dropped and replaced by `settle_usage(p_agency_id, p_period, p_kind, p_reserved, p_landed) returns integer` — an upsert, so a settle addressed to a period with no row yet still counts what landed there: `pending = greatest(pending - p_reserved, 0)`, `count = count + p_landed`, returning the new `count` so the 80 % bell fires on what landed; EXECUTE revoked from public, anon and authenticated and granted to the service role like its siblings. The `count >= 0` check from `supabase/migrations/20260853_billing_followups.sql:57` stays. No policy change: the table's one policy is a member's select. Then `npm run db:types`; until then the two function shapes and the column are hand-added to `src/types/database.ts` (`usage_counters` at `:1785`, `consume_usage` at `:1877`, `refund_usage` at `:1922` removed) in the generator's layout.
- **Deploy window.** Migration first: the old code keeps reserving through the new `consume_usage` (same arguments) but never settles, so a generation in the minutes between the migration and the deploy is a free one and its reservation is cleared by the next daily reset. Code first would call a `settle_usage` that does not exist. Migration first is the harmless order.
- **`src/lib/billing/usage.ts` — two layers, nothing twice.** The RPC pair: `consumeUsage` keeps its signature and reserves; its refusal now carries the `AllowanceError` itself (`{ allowed: false, refused }`), so the error is built in one place instead of at each of its three callers, and its bell moves out. `settleUsage(entitlement, agencyId, kind, reserved, landed)` replaces `refundUsage` — the one way a reservation ends, whether everything landed, nothing did, or a run wrote fewer drafts than it asked for; it clamps `landed` to `reserved` (the one place, not in each caller), carries the 80 % bell (`allowanceWarning`, `src/lib/billing/copy.ts:82`, through `notify`, `src/lib/notifications/notify.ts:66`) on the crossing of `count`, and a failed RPC is logged, never thrown — the picture is already stored, and a settle that is lost is a free image for the customer and a pending unit the daily reset clears. The in-request layer over the pair: `reserveUsage(spender, kind, cost)` — the entitlement read, the compare-and-set, the refusal thrown, the reservation held on the spender by kind — is the one reserve for a provider wrapper or a route (`subscribeFal` for images, the rewrite route for a rewrite); `runMetered(spender, fn)` — `runAsSpender` (`src/lib/billing/spend-context.ts:47`) plus the settle — counts every reservation `fn` made when `fn` resolves and gives them all back when it throws, replacing `releaseCharged` and the rewrite route's hand-rolled reserve-then-settle. A boundary is one landing: everything between the reservation and the stored result runs inside `fn`. `spendFailureResponse(err, scope, fallback, status)` is the one answer to a failed metered spend — the 402 for an `AllowanceError`, else the error's own words behind 502 (provider) or 500 (ours) — replacing the four-line catch that five routes spelled by hand. `readUsage` returns `{ landed, committed }` (both `Allowance`, `src/lib/billing/plans.ts:28`): Settings shows `landed` (`src/app/(dashboard)/settings/page.tsx:69` → `PlanSection`), the cap readers use `committed` — the generate cron's draft budgets (`src/app/api/cron/generate/helpers.ts:99`), the visuals cron's per-agency remainder (`src/app/api/cron/visuals/route.ts:112`), the wizard's `draftsLeft` (`src/app/(generate)/generate/page.tsx:52`). `clearStaleReservations(admin)` — `update usage_counters set pending = 0 where pending > 0 and reserved_at < now() − 10 min` — is the daily reset the billing cron (`src/app/api/cron/billing/route.ts:16`) runs after the document retry: a killed invocation's reservation holds what it reserved against the cap until the next morning and nothing else. The age filter is not optional: the billing cron fires at `0 8 * * *`, the same minute as the hourly generate cron, which reserves a client's whole batch seconds before — a blind reset would zero a live reservation every day and let `count` pass the quota once both settled. Ten minutes is longer than any invocation may live (300 s is the largest `maxDuration`), so a row nothing has reserved on for that long holds only leftovers. The `AdminClient` type the file needs is exported once from `src/lib/supabase/admin.ts` and the three private copies of it (`src/app/api/cron/generate/helpers.ts`, `src/lib/billing/entitled-clients.ts`, `src/lib/billing/reminders.ts`) read it from there.
- **`Spender` (`src/lib/billing/spend-context.ts:35`).** `charged` (`:41`) becomes `reserved: Partial<Record<AllowanceKind, number>>` — what `reserveUsage` holds under this spender, by kind, not yet settled. `runMetered` sets it to `{}` before running `fn`; `reserveUsage` refuses when it is undefined, the same fail-closed rule as "no spender in scope": a paid call under a plain `runAsSpender` would reserve and never settle.
- **`subscribeFal` (`src/lib/visual/fal.ts:46`).** One line for a paid model — `reserveUsage(spender, 'image', 1)` — then the call and the telemetry; the refund-on-throw at `:60-63` goes — the boundary settles. `generateSlideImage`, `generateVectorAsset`, `editImageWithMask` are unchanged; `removeImageBackground` (free model) needs no `runMetered`.
- **Boundaries — `runAsSpender` → `runMetered`, `releaseCharged` and the hand-written catch deleted:** `src/app/api/ai/generate-visual/route.ts:60,108`, `src/app/api/ai/generate-background/route.ts:82,135`, `src/app/api/posts/[id]/visuals/route.ts:42,57`, the visuals cron's per-position job (`src/app/api/cron/visuals/route.ts:205-213`, whose inner try/catch existed only to release). `src/app/api/ai/inpaint/route.ts:83,102` and `src/app/api/ai/generate-svg/route.ts:63,67,87` move their download and upload inside the callback so the landing is what settles them; the SVG route's rejection (`:67`) becomes a private error thrown inside the callback and answered as the same 502 from the catch. The rewrite route (`src/app/api/ai/rewrite/route.ts:77,102`) runs `reserveUsage` then the model inside `runMetered`, and answers a refusal with `allowanceResponse`. `startGenerationRun` (`src/lib/generation/runs.ts:62`) hands back the refusal `consumeUsage` built and settles `(target, 0)` where it refunded (`:86`); `finishGenerationRun` (`:124`) takes `{ agencyId, entitlement, reserved, landed }` instead of `unused` and settles once — its two callers (`src/app/api/cron/generate/route.ts:185`, `src/app/api/ai/generate-stream/route.ts:224`) pass the target and what was written or streamed, unclamped.
- **Guards and registries.** `gate-coverage.test.ts` (`src/lib/billing/__tests__/gate-coverage.test.ts:77`) treats `runMetered(` as a spender declaration beside `runAsSpender(`. `docs/OPERATIONS.md:133` — "Give back … | `refundUsage`" becomes "Settle a reservation — count what landed, give back the rest | `settleUsage`", plus "Clear the reservations a killed invocation never settled (daily cron) | `clearStaleReservations`"; `scripts/table-writers.json:117` names the two RPCs and the reset. Tests: `fal-metering.test.ts` (reserves through `reserveUsage` before the call, nothing else of the ledger), `runs.test.ts` (the refusal handed back, the settle shapes), `usage.test.ts` (new: `reserveUsage` inside `runMetered` reserves, settles as landed on resolve, gives back on throw, settles each kind, throws the refusal before any call, refuses outside `runMetered`; `settleUsage` clamps and rings the bell on the landed crossing; `readUsage` splits landed from committed; `spendFailureResponse`'s three answers; the daily reset's age filter), `src/app/api/ai/__tests__/generate-svg-route.test.ts` (new) for the one route whose control flow changed — generation, download, the gate and the upload as one landing under `runMetered`, a rejected vector thrown inside it and answered as the 502, a failed download in the provider's words. The other boundaries are a swap of `runAsSpender` for `runMetered` and a deleted release line, and are the observed run's.
- **Not changed.** The pessimistic cap: a refusal still happens before fal is paid. The drafts' "count what was produced" rule keeps its meaning — a wizard draft counts when it is streamed, a cron draft when its post is written. The 60 s entitlement cache can, at a renewal, hand the settle a different period key than the reservation used; the settle's upsert counts the landed unit in the period it names and the daily reset releases the old period's pending — noted, not engineered around. A wizard run whose `generation_runs` insert fails for a reason other than a lost slot proceeds without a run and its drafts are never counted (`startGenerationRun` already gave the reservation back) — a rare database failure yields a free run, as before this step.

→ verify: `usage.test.ts`, `fal-metering.test.ts`, `runs.test.ts` green; gate-coverage still counts the metered routes; the migration applied to prod and types regenerated identical to the hand edits; an observed run in the test workspace: a wizard batch whose one slide is made to fail (a wrong `FAL_API_KEY` for one request is enough) leaves the Settings meter at the number of pictures on screen; `select count, pending from usage_counters` shows `pending` back at 0 after the next billing cron.

## Verified before writing

| Symbol | Where | Exported | On error | Cache | Notes |
|---|---|---|---|---|---|
| `getCachedUserRecord`, `USER_RECORD_TAG` | `src/lib/auth/helpers.ts:25-56` | yes | returns null on a missing row, then re-reads uncached | `unstable_cache` 300 s, tag `'user-record'` | the ONE agency-resolution read; `agency_id, role` only |
| `requireAuth` | `src/lib/auth/helpers.ts:68` | yes | throws `AuthError` 401/404 | — | only caller is `resolveAuth` |
| `resolveActionAuth` | `src/lib/auth/helpers.ts:239` | yes | returns `{ ok: false, error }` | via getCachedUserRecord | drops role; 13 action files use it |
| `verifyAdminRole` | `src/lib/auth/helpers.ts:257` | yes | boolean, fresh user-scoped read | none | used by account PUT, invite POST, team-actions |
| `fetchOwnedPost` | `src/lib/auth/helpers.ts:123` | yes | null when not owned | — | publish route's ownership read |
| `resolveAuth` | `src/lib/auth/resolve-auth.ts:24` | yes | returns `{ ok: false, response }` (401/404/500) | — | `AuthContext` has no role, no plan |
| `requireAuthUserId`, `requireSessionUser` | `src/lib/auth/session.ts:29-67` | yes | `redirect(SIGN_IN_PATH)` | header read, no DB | the (generate) and (onboarding) layouts call only `requireAuthUserId` |
| `getCachedAgency` | `src/lib/queries/cache.ts:55` | yes | null on miss (`.single()` error swallowed) | `unstable_cache` 60 s, tag `'agencies'` | full `AGENCY_COLUMNS` row via admin client — the entitlement's input |
| `fetchAgencyById` | `src/lib/queries/db.ts:132` | yes | throws via `unwrap` on a query error, null when no row | none | uncached — the settings page's read after Checkout |
| `unwrap` | `src/lib/queries/unwrap.ts:14` | yes | throws with the query name | — | |
| `AGENCY_COLUMNS`, `AGENCY_SETTINGS_COLUMNS`, `POSTING_SCHEDULE_DUE_COLUMNS` | `src/lib/queries/select-columns.ts:271,280,236` | yes | — | — | both agency projections carry `plan_client_limit` today |
| `SOCIAL_CONNECTION_SYNC_COLUMNS` | `src/lib/queries/select-columns.ts:397` | yes | — | — | roster projection for metrics and comments |
| `VISUAL_BACKLOG_POST_COLUMNS` | `src/lib/queries/select-columns.ts:638` | yes | — | — | visuals cron backlog projection |
| `AgencyInfo`, `NotificationType` | `src/types/api.ts:292-333` | yes | — | — | `AgencyInfo` is the last hand-written mirror; readers: plan-section, settings-view, account-tab, db.ts |
| `KNOWN_MIRRORS` | `src/types/__tests__/row-mirrors.test.ts:96` | private | — | — | backlog list; `'types/api.ts:AgencyInfo'` is its only entry |
| `POLICYLESS` | `src/app/__tests__/rls-policies.test.ts:55` | private | — | — | empty; every table in any migration needs a policy |
| `EXEMPT` | `src/app/api/__tests__/boundary-validation.test.ts:42` | private | — | — | webhook route joins it with a reason |
| `startGenerationRun`, `finishGenerationRun` | `src/lib/generation/runs.ts:51-105` | yes | degrade: log + `{ runId: null, slotTaken }` / log | — | slot dedup via unique index; callers: cron generate, generate-stream |
| `trackGenerationTheme` | `src/lib/generation/runs.ts:220` | yes | — | — | writes `generation_themes.post_count` |
| `subscribeFal` | `src/lib/visual/fal.ts:50` | private | throws (fal detail attached); `AllowanceError` when the pool is empty; refuses with no spender | reads `getCachedEntitlement` | "All model invocations go through here" — the metering point since step 3 |
| `FAL_MODEL`, `DIS_MODEL`, `EDIT_MODEL`, `VECTOR_MODEL` | `src/lib/visual/fal.ts:6-9` | private | — | — | cutouts (`DIS_MODEL`) stay free |
| `generateSlideImage`, `removeImageBackground`, `generateVectorAsset`, `editImageWithMask` | `src/lib/visual/fal.ts:120-170` | yes | throw | — | callers: generate-visual.ts:96, isolate-subject:35, generate-svg:52, inpaint:73 |
| `generateVisual` | `src/lib/visual/generate-visual.ts:56` | yes | throws on fal/download failure; `AllowanceError` from `subscribeFal` | — | identity from the spender context since step 3; callers: generate-visual route, generate-background route, generate-post-visual |
| `callAnthropic`, `anthropic`, `DEFAULT_MODEL`, `LIGHT_MODEL` | `src/utils/ai-client.ts:8-84` | yes | throws after 3 retries; streams via `messages.stream`, returns `finalMessage()` (carries `usage`) | — | 14 caller files, all in `src/ai` plus `fetch-trend-search.ts`, `describe-palette.ts`, `utils/ai.ts`; no agency reaches it today |
| `queryTavily` | `src/lib/sources/tavily-client.ts:29` | yes | throws | — | the one Tavily call site |
| `resolveNarrative` | `src/features/analytics/lib/shared/narrative-shared.ts:153` | yes | — | called inside `unstable_cache` from `narrative.ts:116` | calls `generateAnalyticsSummary` at :175 |
| `generateAnalyticsSummary` | `src/ai/analytics/generate-summary.ts` | yes | — | — | Haiku narrative, view-triggered |
| `refreshStyleMemo` | `src/features/clients/actions/style-memo-actions.ts:16` | yes | `ActionResult` | — | calls `distillStyleMemo` with the admin client at :31 — a spend boundary |

| `generatePostVisual` | `src/lib/visual/generate-post-visual.ts:26` | yes | typed refusal `not_found`/`no_copy`; throws otherwise | — | shared by the visuals route and the visuals cron |
| `pickVisualBacklog`, `totalVisualSlots`, `BacklogPost` | `src/lib/visual/visual-backlog.ts:14-49` | yes | pure | — | |
| `GET` (generate cron) | `src/app/api/cron/generate/route.ts:40` | yes | 401 without bearer; 500 on the two roster queries | — | `TIME_BUDGET_MS` (:28) private; `dueClients` flatMap at :127; `startGenerationRun` at :172; slide count at :163 |
| `fetchScheduleContext`, `getScheduleDue` | `src/app/api/cron/generate/helpers.ts:51,158` | yes | throws on any context query error | — | agencies select is `id, timezone, mode` (:58-59) |
| `GET` (visuals cron) | `src/app/api/cron/visuals/route.ts:43` | yes | 401; 500 on the backlog query | — | `MAX_IMAGES_PER_RUN` (:16), `MAX_VISUAL_ATTEMPTS` (:17), `BACKLOG_FETCH_LIMIT` (:35) private; `countAttempt` (:115) before `generatePostVisual` (:154) |
| `publishDuePosts` | `src/features/publishing/lib/scheduler.ts:93` | yes | throws on the due query error | — | `BATCH_LIMIT` (:67) private; `posts!inner` embed filtered by `.lte('posts.scheduled_at')` at :171; per-(client, platform) loop at :242 |
| `syncRoster`, `MetricsSyncOutcome` | `src/features/analytics/lib/shared/sync-shared.ts:22-100` | yes | throws on the roster query error | — | serves Instagram and Facebook metrics; `outcome.skipped` exists |
| `syncAllClientComments` | `src/features/comments/lib/sync-comments.ts:81` | yes | throws on the roster query error | — | its own roster read (:95-105), same projection |
| `refreshExpiringTokens` | `src/features/publishing/lib/refresh-tokens.ts:48` | yes | — | — | deliberately NOT gated |
| `checkRateLimit`, `aiRateLimitResponse`, `visualsRateLimitResponse` | `src/lib/auth/rate-limit.ts:47-89` | yes | `NextResponse | null` | in-memory per instance | "NOT a spend ceiling" — 13 route callers |
| `POST` (generate-stream) | `src/app/api/ai/generate-stream/route.ts:37` | yes | JSON errors | — | `startGenerationRun` at :84 before research at :119 |
| `createClient`, `updateClient`, `deleteClient` | `src/features/clients/actions/client-actions.ts:57,121,176` | yes | `ActionResult` failure | busts `'agency-clients'` | `createClient` passes the user-scoped client to `provisionClient` at :58; no cap today |
| `provisionClient` | `src/features/clients/lib/provision-client.ts:42` | yes | `{ ok: false, error }` | — | only caller is `createClient` |
| `notify`, `NOTIFY_EVERY_TIME` | `src/lib/notifications/notify.ts:32,66` | yes | throws on the cooldown read; resolves 'written' / 'suppressed' / 'failed' (an insert failure is logged, never thrown) | — | dedup by exact `message` within `cooldownDays` (default 7); the billing cron mails only behind 'written' |
| `scheduleInputSchema` | `src/features/clients/schemas.ts:45` | yes | zod | — | `frequency_value` (:48) and `posts_per_week` (:56) are `positive()` with no max |
| `generateStreamSchema` | `src/features/generate/schemas.ts:110` | yes | zod | — | `slideCount` clamped (:116); `targetPostCount` `min(0)` only (:117); `priorityPosts` unbounded (:118) |
| `MAX_CAROUSEL_SLIDES`, `MIN_CAROUSEL_SLIDES`, `DEFAULT_CAROUSEL_SLIDES`, `QUALITY_FLOOR`, `MS_PER_DAY` | `src/utils/constants.ts:4-19` | yes | — | — | |
| `POSTS_PER_RUN_OPTIONS` | `src/utils/constants.ts:152` | yes | — | — | UI offers 1–7 |
| `PlanSection` | `src/features/settings/components/plan-section.tsx:40` | yes | — | server component | rewritten in step 7's first half: entitlement + usage meters; `Meter` private |
| `ShellProvider`, `useShell` | `src/components/layout/shell-context.tsx:82-234` | yes | `useShell` throws outside the provider | — | |
| `createUserRecord` | `src/lib/auth/create-user-record.ts:48` | yes | throws | — | inserts `{ name, mode }` only (:99-101); billing columns come from DB defaults |
| `createAdminSupabaseClient`, `AdminClient` | `src/lib/supabase/admin.ts:14,28` | yes | — | — | the type is exported since step 13, replacing four private copies |
| `sendEmail` | `src/lib/email/resend.ts:39` | yes | throws with the provider's words | — | the one Resend send path (step 8); `senderAddress` (:17) private, throws without `RESEND_FROM_EMAIL` |
| `approvalEmail`, `confirmSignupEmail` | `src/lib/email/templates.ts:21,105` | yes | pure | — | snapshot-tested in `src/lib/email/__tests__/templates.test.ts` |
| `schedulePosts` | `src/lib/actions/post-actions.ts:432` | yes | `ActionResult` | — | need `'publish'` |
| `PUT` (account settings) | `src/app/api/settings/account/route.ts:26` | yes | JSON errors | busts `'agencies'` at :59 | user-scoped `agencies` update of name/timezone at :53 |
| `POST` (team invite) | `src/app/api/settings/team/invite/route.ts:19` | yes | JSON errors | — | admin check only; no mode or seat rule — unchanged |
| `ActionResult` | `src/lib/actions/types.ts:2` | yes | — | — | `{ ok: false, error: string }` — no code field; the UI shows the message |
| `consume_image_credits`, `refund_image_credits` | `supabase/migrations/00000000_baseline.sql:1090-1136` | — | SECURITY DEFINER, default EXECUTE TO PUBLIC | — | compare-and-set; generalised and dropped in step 1 |
| `image_generation_usage` | `supabase/migrations/00000000_baseline.sql:263` | — | — | — | PK (agency_id, month); policy in `supabase/migrations/20260832_close_the_policyless_tables.sql:50` |
| `agencies_member_access`, `users_self_access`, `clients_agency_isolation` | `supabase/migrations/20260818_capture_rls_policy_baseline.sql:25-38` | — | FOR ALL, no WITH CHECK, no column grants | — | why the tenant can write billing columns today |
| `shellNotice`, `workspacePaused`, `REMINDER_COPY`, `BILLING_NOTIFICATION_TITLES` | `src/lib/billing/copy.ts:269,312,326,375` | yes | pure | — | every billing sentence; `CHECKOUT_CONSENT` and the `payment_failed` copy join it (step 12) |
| `Entitlement`, `EntitlementState`, `allows`, `noEntitlement`, `entitlementFor` | `src/lib/billing/entitlement.ts:44-131` | yes | pure | — | the row's ONLY interpreter; gains `endsOn` (step 12) |
| `PRO_PLAN`, `TRIAL_BRANDS`, `GRACE_DAYS`, `PLAN_LABELS` | `src/lib/billing/plans.ts:31-49` | yes | — | — | prices in euro cents; the one paid plan since step 9 (`PLANS` with `starter` and `agency` is gone) |
| `isPlanId` | `src/lib/billing/entitlement.ts:83` | private | pure | — | names the plan ids the row may carry — `pro` replaces the two |
| `brandCapReached` | `src/lib/billing/copy.ts:101` | private | pure | — | the cap sentence says "Move to Agency" today; on `pro` the cap is never reached |
| `addBrandRefusal` | `src/lib/billing/copy.ts:112` | yes | pure | — | the one rule for the create action and the button that leads to it |
| `pickReminder`, `remindTrialWorkspaces` | `src/lib/billing/reminders.ts:39,80` | yes | throws on the two roster reads; per-workspace failures collected | — | the bell-then-email pair the webhook's `payment_failed` reuses via `remindWorkspace` |
| `reminderEmail` | `src/lib/email/templates.ts:56` | yes | pure | — | file snapshots per kind; `documentEmail` joins it |
| `escapeHtml`, `strong`, `EmailContent`, `renderEmail` | `src/lib/email/layout.ts:44-85` | yes | pure | — | tables + inline styles, the discipline the document HTML follows |
| `getCachedEntitlement` | `src/lib/queries/cache.ts:64` | yes | `noEntitlement()` on a missing row | inherits `'agencies'` 60 s | |
| `countClientsByAgency`, `fetchTeamMembersByAgency` | `src/lib/queries/db.ts:70,162` | yes | throw via `unwrap` | — | the brand count for the checkout rules; the `unwrap` shape for `fetchSaleDocumentsByAgency` |
| `AGENCY_BILLING_KEYS` | `src/lib/queries/select-columns.ts:246` | private | — | — | the one list of billing columns; the keys are spread into three exported projections |
| `AgencyBillingColumns`, `AGENCY_ENTITLEMENT_COLUMNS`, `USER_CONTACT_COLUMNS` | `src/lib/queries/select-columns.ts:287-354` | yes | — | — | `SALE_DOCUMENT_COLUMNS` joins the file |
| `removeTeamMember` | `src/features/settings/actions/team-actions.ts:21` | yes | `ActionResult` | busts `USER_RECORD_TAG` | keeps its fresh `verifyAdminRole` read; the billing actions use the cached role instead |
| `removeTeamMemberSchema`, `accountSettingsSchema` | `src/features/settings/schemas.ts:11-23` | yes | zod | — | no new schema: the two billing actions take no input |
| `USER_AUTH_COLUMNS` | `src/lib/queries/select-columns.ts:351` | yes | — | — | `agency_id, role` — the role `resolveActionAuth` will return |
| `getUserRecord` | `src/lib/auth/helpers.ts:85` | private | null on a missing row | — | the read behind `getCachedUserRecord` |
| `IDENTIFIES_CALLER` | `src/app/__tests__/rls-policies.test.ts:28` | private | — | — | why `document_counters` takes a `POLICYLESS` entry rather than a `using (false)` policy |
| `AccountTab`, `AccountRail` | `src/features/settings/components/account-tab.tsx:64,157` | yes | client component; `fetch` to the PUT | — | `PlanActions` and `BillingDocuments` sit beside them on the Account tab |
| `SettingsView` | `src/features/settings/components/settings-view.tsx:59` | yes | client | — | its `useEffect` at :81 handles `canva_connected` — `billing=` joins it |
| `PANEL_COPY` | `src/features/settings/components/settings-view.tsx:24` | private | — | — | |
| `nextDate`, `Meter` | `src/features/settings/components/plan-section.tsx:44,116` | private | pure | — | "Ends on" lands in `nextDate` |
| `SettingsPage` | `src/app/(dashboard)/settings/page.tsx:23` | yes | `redirect(SIGN_IN_PATH)` without an agency | uncached `fetchAgencyById` | the return page after Checkout |
| `DashboardLayout` | `src/app/(dashboard)/layout.tsx:59` | yes | — | cached reads | passes `shellNotice` to the banner |
| `GET` (billing cron), `maxDuration` | `src/app/api/cron/billing/route.ts:6-13` | yes | 500 on a throw, errors logged | — | grows the two backstops (step 11) |
| `unauthorizedCron` | `src/lib/cron/authorize-cron.ts:8` | yes | a 401 response, or null | — | |
| `readUsage`, `AllowanceError` | `src/lib/billing/usage.ts:128,166` | yes | throws on the read | — | |
| `requireEntitledRoute`, `requireEntitledAction` | `src/lib/billing/require-entitled.ts:24,37` | yes | 402 or the failure, else null | via `getCachedEntitlement` | the billing actions spend nothing — no gate; `gate-coverage` keys on `runAsSpender`, which they never call |
| `GATED`, `EXEMPT` (gate coverage) | `src/lib/billing/__tests__/gate-coverage.test.ts:18,50` | private | — | — | unchanged by Phase 2 |
| `EXEMPT` (cron invariants) | `src/app/api/cron/__tests__/cron-invariants.test.ts:156` | private | — | — | `billing` already listed |
| `KNOWN_UNVALIDATED` | `src/app/api/__tests__/boundary-validation.test.ts:59` | private | — | — | stays empty; the webhook joins `EXEMPT` |
| `tables` | `src/app/__tests__/rls-policies.test.ts:107` | private | — | — | reads `create table` from every migration — the two new tables need their policies in 20260855 |
| `POST` (data deletion) | `src/app/api/meta/data-deletion/route.ts:30` | yes | 403 on a bad signature, 500 on a failed erasure | — | the raw-body-then-verify shape the webhook follows |
| `verifySignedRequest` | `src/app/api/meta/data-deletion/route.ts:8` | private | null on a bad signature | — | |
| `getBrowser` | `src/lib/render/browser.ts:29` | yes | throws if Chromium cannot launch | module-scoped warm browser | `CHROME_EXECUTABLE_PATH` locally, `@sparticuz/chromium` on Vercel |
| `uploadToBucket` | `src/features/assets/lib/storage.ts:22` | private | throws | — | public buckets only (returns a public URL) — not reused for the private one |
| `after` | `src/app/api/extract/start/route.ts:1,64` | imported | — | — | work after the response; also `src/app/api/posts/[id]/publish/route.ts:183` |
| `POST_IMAGES_BUCKET`, `CLIENT_FILES_BUCKET`, `PLAN_AND_BILLING_PATH` | `src/utils/constants.ts:50-63` | yes | — | — | `BILLING_DOCUMENTS_BUCKET` joins them |
| `resolveAppUrl`, `clearQueryParams` | `src/utils/url.ts:2,13` | yes | — | — | |
| `formatLongDate`, `pluralise` | `src/utils/format.ts:16,129` | yes | pure | — | |
| `BillingReminderType`, `SettingsTab` | `src/types/api.ts:307,339` | yes | — | — | `BillingReminderType` widens to `payment_failed` |
| `PILL_TONES` | `src/components/ui/status-pill.tsx:10` | yes | — | — | |
| `buttonClasses`, `Button` | `src/components/ui/button.tsx:35,55` | yes | — | — | variants primary / secondary / ghost / danger; `PlanActions` uses one primary |
| `FormSection`, `SaveBar`, `RailBox`, `RailStat`, `RailText` | `src/components/ui/form/index.ts:1-7` | yes | — | — | re-exported from their files |
| `ActionLink` | `src/components/ui/action-link.tsx:29` | yes | — | — | |
| `HAND_LISTED` | `scripts/table-writers.mjs:105` | private | — | — | the `[hand-listed]` prefix for writers that go through an RPC |
| `billing_events` | `supabase/migrations/20260852_billing_foundation.sql:237` | — | — | — | id = the Stripe event id; its admin-read policy at `supabase/migrations/20260853_billing_followups.sql:38` is the predicate `sale_documents_admin_read` copies |
| `formatZodIssues` | `src/lib/validation/format-issues.ts:13` | yes | — | — | |
| `toDateKey`, `getZonedParts` | `src/utils/date-helpers.ts:43,55` | yes | pure; cached `Intl.DateTimeFormat` per zone | — | the QR's date and time; `getZonedParts` gains `second` |
| `TablesInsert` | `src/types/database.ts:1968` | yes | — | — | the RPC input's type — no hand-written mirror |
| `READS_BODY` | `src/app/api/__tests__/boundary-validation.test.ts:23` | private | — | — | matches `request.json()` only — the webhook's `request.text()` needs no exemption |
| `outputFileTracingIncludes` | `next.config.ts:68` | — | — | — | per-function force-include of the Chromium binary; the webhook and the billing cron join the two routes listed |
| `consumeUsage`, `settleUsage`, `readUsage`, `clearStaleReservations`, `reserveUsage`, `runMetered`, `allowanceResponse`, `spendFailureResponse` | `src/lib/billing/usage.ts:44,82,128,155,189,210,234,257` | yes | `consumeUsage`, `readUsage` and `clearStaleReservations` throw on a query error; `reserveUsage` throws the refusal; `settleUsage` logs a lost settle | `reserveUsage` and `runMetered` read `getCachedEntitlement` | step 13's ledger — before it, `refundUsage` (`:71`) and `releaseCharged` (`:128`) were the refund half, read at those lines before the rewrite |
| `subscribeFal` | `src/lib/visual/fal.ts:46` | private | throws; before step 13 it refunded its own reservation on a fal throw | `getCachedEntitlement` | the one metering point; the boundary settles |
| `Spender`, `runAsSpender`, `currentSpender` | `src/lib/billing/spend-context.ts:35,51,56` | yes | pure | — | `charged` became `reserved` in step 13 |
| `startGenerationRun`, `finishGenerationRun` | `src/lib/generation/runs.ts:62,115` | yes | reserve refused → a typed claim; `giveBack` at `:86` on a failed insert | — | `finishGenerationRun` took `unused` before step 13; callers at `src/app/api/cron/generate/route.ts:185` and `src/app/api/ai/generate-stream/route.ts:224` |
| `POST` (rewrite) | `src/app/api/ai/rewrite/route.ts:55` | yes | reserves at `:77`, refunds at `:102` | — | the one non-image, non-draft reservation |
| `GET` (visuals cron) | `src/app/api/cron/visuals/route.ts:55` | yes | 500 on the backlog query | — | `readUsage` at `:112`; per-position `runAsSpender` at `:205` with the release at `:211` |
| `fetchScheduleContext` | `src/app/api/cron/generate/helpers.ts:51` | yes | throws | — | draft budgets from `readUsage` at `:99` |
| `GET` (billing cron) | `src/app/api/cron/billing/route.ts:17` | yes | 500 on a throw | — | the document retry is where the daily reset joined |
| `usage_counters`, `consume_usage`, `refund_usage` | `supabase/migrations/20260852_billing_foundation.sql:84,97,127` | — | `consume_usage` is a plpgsql compare-and-set; `refund_usage` a clamped update | — | `count >= 0` added by `supabase/migrations/20260853_billing_followups.sql:57` |
| `Allowance`, `ALLOWANCE_WARN_SHARE` | `src/lib/billing/plans.ts:28,46` | yes | — | — | the shape `readUsage` returns twice over |
| `allowanceWarning` | `src/lib/billing/copy.ts:82` | yes | pure | — | the 80 % sentence, moving from reserve to settle |
| `GATED`, `EXEMPT`, the spender scan | `src/lib/billing/__tests__/gate-coverage.test.ts:18,50,77` | private | — | — | the regex at `:77` gains `runMetered` |

## New things — grep by shape (2026-09-13)

- `entitle`: hits only in font/lockup/brand-style code and Meta auth — no entitlement helper exists.
- `allowance` / `quota`: Meta publishing quota, ideas rate cap, prompt budgets, the retired image counter — no plan allowance exists.
- `billing`: `terms/page.tsx` copy and `plan-section.tsx` — no billing module, no `src/app/api/billing`.
- `stripe`: `database.ts` (the two columns), `select-columns.ts`, and the words "grey stripe" in `queue-rail.tsx` / `busy-hint.tsx` — no `stripe` dependency in `package.json`.
- `sendEmail`, `BillingWall`, `billing-banner`: none.
- Writers of `agencies`: `create-user-record.ts` (insert) and `settings/account/route.ts` (name, timezone) — nothing writes the billing columns.

## New things — grep by shape (2026-09-16, Phase 2)

- `stripe` in `src`: only the readers of the two columns (`select-columns.ts`, `cache.ts`, `entitlement.ts`, `reminders.ts`, `plans.ts` comments) and "grey stripe" in two components; no `stripe` in `package.json`; no `src/app/api/billing`.
- `qrcode` / `QRCode`: none in `src` or `package.json`.
- PDF writing (`page.pdf(`): none — `pdf-parse` reads PDFs (`src/lib/sources/extract-text.ts:16`), nothing writes one; the only Chromium user is the capture pipeline through `getBrowser`.
- `createSignedUrl`: none — every bucket today is public.
- `attachments` on an email: none — `sendEmail` sends HTML only.
- `invoice` / `receipt` / `фактура` in `src`: comments in the review and insight code and in `ai-prices.ts`; no document, numbering or counter exists (`sale_documents`, `document_counters`, `billing-documents`: none anywhere).
- XML builders: `robots.ts`, the SVG route, the sanitiser — nothing builds an XML document; the audit file is string templating with escaping.
- `after(` from `next/server`: two routes (`extract/start`, `posts/[id]/publish`) — the pattern exists.
- `tsx`: not a dependency, and stays out — the catalogue is made by hand in the Dashboard; the audit file is a route, not a script, because the admin client is `server-only`.
- Company identity: spelled by hand in the two legal pages and the footer; `COMPANY` in `src/utils/constants.ts` becomes the one the documents read (the marketing copies are noted, not touched).
- `Intl.NumberFormat` / currency formatting: none in `src` (the only `€` is a comment in `plans.ts`) — `formatMoney` is new in `src/utils/format.ts`, with three readers from day one.
- XML escaping: `escapeHtml` in `src/lib/email/layout.ts:44` already escapes the five XML entities — reused, not re-implemented.
- Zoned date parts: `toDateKey` and `getZonedParts` in `src/utils/date-helpers.ts:43,55` — the QR's date and time come from them.
- Admin-only action guard: `resolveActionAuth` already holds the role and drops it — returning it is the change; no wrapper, no second read.

## Verification (end to end)

1. After step 1 on a prod copy: the anon-key PostgREST update of `agencies.plan` returns 42501; `PUT /api/settings/account` still works; `usage_counters` image count equals the dropped table's; `npm run check` green.
2. After step 6: a fixture agency past its grace gets 402 from every gated route, its crons report `skipped_unentitled` with **zero** Anthropic/fal/Graph calls (log inspection), while `refresh-tokens` still refreshes its token.
3. After step 7: the manual browser matrix trial → ending → grace → locked → (Phase 2) paid → add brand → cancel at period end, both modes, desktop and mobile.
4. Phase 2: the `stripe listen` end-to-end above in test mode; no live key is set anywhere until Phase 3 closes.
5. Deploy order: migration → `npm run db:types` → code. `npm run check` (typecheck, lint, format:check, knip, arch, writers, comments, coverage) green at every commit; `npm run lint` is run on its own, never through a pipe.
6. Phase 2 in test mode: the `stripe listen` matrix of step 9; a paid invoice → a PDF in the bucket → the email with it attached → the document listed in Settings; a credit note → its document; the audit file for the test month downloaded from the route with the cron bearer and checked by hand against the two documents; the failing card → bell, email, banner; cancel at period end → "Ends on".
7. Before live: the hand list of step 12 done in order; `NRA_ESHOP_NUMBER` and `STRIPE_ACCOUNT_ID` set — the seller module refuses to render without them, so no document can carry an empty QR; the first real audit file accepted by the NRA portal's validator.

## Left for Phase 3

The public pricing page and the Terms/Privacy rewrite (mock first, outside the dashboard — the Privacy page names the ten-year retention of a customer's name and address on the documents) · the live-key switch after the hand work in step 12 · a Bulgarian-language build of the documents if a customer asks (the labels are bilingual already).
